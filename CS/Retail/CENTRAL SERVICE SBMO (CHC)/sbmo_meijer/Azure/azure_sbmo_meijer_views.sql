 IF NOT EXISTS(SELECT * FROM sys.schemas WHERE [name] = N'SBMO_MEIJER')      
     EXEC (N'CREATE SCHEMA SBMO_MEIJER')                                   
 GO                                                               
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'ADDRESSES' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[ADDRESSES]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.ADDRESSES (
   TENANT_ID, 
   ID, 
   ADDRESS, 
   ADDRESS_LINE_2, 
   CITY, 
   STATE, 
   ZIP, 
   OWNER_ID, 
   IDX, 
   COUNTRY, 
   LAST_UPDATED_DATE, 
   LAST_UPDATED_USER_ID)
AS 
   SELECT 
      ADDRESSES.TENANT_ID, 
      ADDRESSES.ID, 
      ADDRESSES.ADDRESS, 
      ADDRESSES.ADDRESS_LINE_2, 
      ADDRESSES.CITY, 
      ADDRESSES.STATE, 
      ADDRESSES.ZIP, 
      ADDRESSES.OWNER_ID, 
      ADDRESSES.IDX, 
      ADDRESSES.COUNTRY, 
      ADDRESSES.LAST_UPDATED_DATE, 
      ADDRESSES.LAST_UPDATED_USER_ID
   FROM SBMO.ADDRESSES
   WHERE ADDRESSES.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.ADDRESSES',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'ADDRESSES'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'ADJUSTMENT_CODES' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[ADJUSTMENT_CODES]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.ADJUSTMENT_CODES (
   ID, 
   CODE, 
   LAST_UPDATED_DATE, 
   DEACTIVATED_DATE, 
   DESCRIPTION, 
   CREATED_DATE, 
   CHANGE_ON_HAND)
AS 
   SELECT 
      ADJUSTMENT_CODES.ID, 
      ADJUSTMENT_CODES.CODE, 
      ADJUSTMENT_CODES.LAST_UPDATED_DATE, 
      ADJUSTMENT_CODES.DEACTIVATED_DATE, 
      ADJUSTMENT_CODES.DESCRIPTION, 
      ADJUSTMENT_CODES.CREATED_DATE, 
      ADJUSTMENT_CODES.CHANGE_ON_HAND
   FROM SBMO.ADJUSTMENT_CODES
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.ADJUSTMENT_CODES',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'ADJUSTMENT_CODES'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'ADJUSTMENT_GROUP' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[ADJUSTMENT_GROUP]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.ADJUSTMENT_GROUP (
   TENANT_ID, 
   ID, 
   APPLIED_DATE, 
   CREATED_DATE, 
   INVENTORY_ID, 
   WS_CLIENT_CERT_IDENTIFIER)
AS 
   SELECT 
      ADJUSTMENT_GROUP.TENANT_ID, 
      ADJUSTMENT_GROUP.ID, 
      ADJUSTMENT_GROUP.APPLIED_DATE, 
      ADJUSTMENT_GROUP.CREATED_DATE, 
      ADJUSTMENT_GROUP.INVENTORY_ID, 
      ADJUSTMENT_GROUP.WS_CLIENT_CERT_IDENTIFIER
   FROM SBMO.ADJUSTMENT_GROUP
   WHERE ADJUSTMENT_GROUP.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.ADJUSTMENT_GROUP',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'ADJUSTMENT_GROUP'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'AUDIT_LOG' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[AUDIT_LOG]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.AUDIT_LOG (
   TENANT_ID, 
   ID, 
   ACTION, 
   NEW_VALUE, 
   OBJECT_NAME, 
   OBJECT_ID, 
   OLD_VALUE, 
   PROPERTY, 
   TIME_STAMP, 
   USER_ID, 
   CREATED_DATE, 
   FACILITY_ID)
AS 
   SELECT 
      AUDIT_LOG.TENANT_ID, 
      AUDIT_LOG.ID, 
      AUDIT_LOG.ACTION, 
      AUDIT_LOG.NEW_VALUE, 
      AUDIT_LOG.OBJECT_NAME, 
      AUDIT_LOG.OBJECT_ID, 
      AUDIT_LOG.OLD_VALUE, 
      AUDIT_LOG.PROPERTY, 
      AUDIT_LOG.TIME_STAMP, 
      AUDIT_LOG.USER_ID, 
      AUDIT_LOG.CREATED_DATE, 
      AUDIT_LOG.FACILITY_ID
   FROM SBMO.AUDIT_LOG
   WHERE AUDIT_LOG.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.AUDIT_LOG',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'AUDIT_LOG'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'BIN_UPC_LINK' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[BIN_UPC_LINK]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.BIN_UPC_LINK (
   TENANT_ID, 
   ID, 
   BIN_BARCODE, 
   DRUG_UPC, 
   DEVICE_NUMBER, 
   DRUG_NAME, 
   NDC, 
   NDC_FMT)
AS 
   SELECT 
      BIN_UPC_LINK.TENANT_ID, 
      BIN_UPC_LINK.ID, 
      BIN_UPC_LINK.BIN_BARCODE, 
      BIN_UPC_LINK.DRUG_UPC, 
      BIN_UPC_LINK.DEVICE_NUMBER, 
      BIN_UPC_LINK.DRUG_NAME, 
      BIN_UPC_LINK.NDC, 
      BIN_UPC_LINK.NDC_FMT
   FROM SBMO.BIN_UPC_LINK
   WHERE BIN_UPC_LINK.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.BIN_UPC_LINK',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'BIN_UPC_LINK'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'CARRIER' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[CARRIER]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.CARRIER (
   TENANT_ID, 
   ID, 
   NAME, 
   SHIP_IDENT, 
   SHIP_PRIORITY)
AS 
   SELECT 
      CARRIER.TENANT_ID, 
      CARRIER.ID, 
      CARRIER.NAME, 
      CARRIER.SHIP_IDENT, 
      CARRIER.SHIP_PRIORITY
   FROM SBMO.CARRIER
   WHERE CARRIER.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.CARRIER',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'CARRIER'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'CF_SEC_CHAIN' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[CF_SEC_CHAIN]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.CF_SEC_CHAIN (
   TENANT_ID, 
   ID, 
   LAST_UPDATED_DATE, 
   CHAIN_PASSWORD)
AS 
   SELECT CF_SEC_CHAIN.TENANT_ID, CF_SEC_CHAIN.ID, CF_SEC_CHAIN.LAST_UPDATED_DATE, CF_SEC_CHAIN.CHAIN_PASSWORD
   FROM SBMO.CF_SEC_CHAIN
   WHERE CF_SEC_CHAIN.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.CF_SEC_CHAIN',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'CF_SEC_CHAIN'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'CF_SEC_STORE' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[CF_SEC_STORE]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.CF_SEC_STORE (
   TENANT_ID, 
   ID, 
   LOAD_DATE, 
   PASS_PHRASE, 
   PRIVATE_KEY, 
   REGISTRATION_DATE, 
   SSL_CERTIFICATE, 
   CHAIN_ID, 
   REG_STATUS)
AS 
   SELECT 
      CF_SEC_STORE.TENANT_ID, 
      CF_SEC_STORE.ID, 
      CF_SEC_STORE.LOAD_DATE, 
      CF_SEC_STORE.PASS_PHRASE, 
      CF_SEC_STORE.PRIVATE_KEY, 
      CF_SEC_STORE.REGISTRATION_DATE, 
      CF_SEC_STORE.SSL_CERTIFICATE, 
      CF_SEC_STORE.CHAIN_ID, 
      CF_SEC_STORE.REG_STATUS
   FROM SBMO.CF_SEC_STORE
   WHERE CF_SEC_STORE.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.CF_SEC_STORE',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'CF_SEC_STORE'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'CHAINS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[CHAINS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.CHAINS (
   TENANT_ID, 
   ID, 
   NHINID, 
   NAME, 
   CHAIN_MARK_UP_PERCENT, 
   CHAIN_DISCOUNT_PERCENT, 
   PARENT_ID, 
   HQ_ID, 
   MINIMUM_PRICE_OVERRIDE, 
   LABEL_TEMPLATE, 
   SHIPPING_ADD_ON_TYPE, 
   SHIPPING_ADD_ON_AMOUNT, 
   USE_ADHERIS, 
   ADHERIS_URL, 
   ADHERIS_TIMEOUT, 
   ADHERIS_VERSION, 
   DEACTIVATED_DATE, 
   ADHERIS_RETRY_COUNT, 
   ADHERIS_RETRY_OPTION)
AS 
   SELECT 
      CHAINS.TENANT_ID, 
      CHAINS.ID, 
      CHAINS.NHINID, 
      CHAINS.NAME, 
      CHAINS.CHAIN_MARK_UP_PERCENT, 
      CHAINS.CHAIN_DISCOUNT_PERCENT, 
      CHAINS.PARENT_ID, 
      CHAINS.HQ_ID, 
      CHAINS.MINIMUM_PRICE_OVERRIDE, 
      CHAINS.LABEL_TEMPLATE, 
      CHAINS.SHIPPING_ADD_ON_TYPE, 
      CHAINS.SHIPPING_ADD_ON_AMOUNT, 
      CHAINS.USE_ADHERIS, 
      CHAINS.ADHERIS_URL, 
      CHAINS.ADHERIS_TIMEOUT, 
      CHAINS.ADHERIS_VERSION, 
      CHAINS.DEACTIVATED_DATE, 
      CHAINS.ADHERIS_RETRY_COUNT, 
      CHAINS.ADHERIS_RETRY_OPTION
   FROM SBMO.CHAINS
   WHERE CHAINS.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.CHAINS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'CHAINS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'CHARGE_PAYMENT_INFO' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[CHARGE_PAYMENT_INFO]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.CHARGE_PAYMENT_INFO (
   TENANT_ID, 
   ID, 
   ORDER_ID, 
   CHARGE_TYPE, 
   CHARGE_IDENT, 
   CHARGE_AMOUNT, 
   CREATED_DATE)
AS 
   SELECT 
      CHARGE_PAYMENT_INFO.TENANT_ID, 
      CHARGE_PAYMENT_INFO.ID, 
      CHARGE_PAYMENT_INFO.ORDER_ID, 
      CHARGE_PAYMENT_INFO.CHARGE_TYPE, 
      CHARGE_PAYMENT_INFO.CHARGE_IDENT, 
      CHARGE_PAYMENT_INFO.CHARGE_AMOUNT, 
      CHARGE_PAYMENT_INFO.CREATED_DATE
   FROM SBMO.CHARGE_PAYMENT_INFO
   WHERE CHARGE_PAYMENT_INFO.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.CHARGE_PAYMENT_INFO',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'CHARGE_PAYMENT_INFO'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'CONTACTS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[CONTACTS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.CONTACTS (
   TENANT_ID, 
   ID, 
   FIRST_NAME, 
   LAST_NAME, 
   TITLE, 
   ENTITY_ID, 
   IDX)
AS 
   SELECT 
      CONTACTS.TENANT_ID, 
      CONTACTS.ID, 
      CONTACTS.FIRST_NAME, 
      CONTACTS.LAST_NAME, 
      CONTACTS.TITLE, 
      CONTACTS.ENTITY_ID, 
      CONTACTS.IDX
   FROM SBMO.CONTACTS
   WHERE CONTACTS.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.CONTACTS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'CONTACTS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'CUSTOM_SCHEDULED_TASK' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[CUSTOM_SCHEDULED_TASK]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.CUSTOM_SCHEDULED_TASK (
   TENANT_ID, 
   ID, 
   TASK_TYPE, 
   TASK_DAY, 
   TRIGGER_TIME, 
   SCHEDULE_GROUPING, 
   FACILITY_ID)
AS 
   SELECT 
      CUSTOM_SCHEDULED_TASK.TENANT_ID, 
      CUSTOM_SCHEDULED_TASK.ID, 
      CUSTOM_SCHEDULED_TASK.TASK_TYPE, 
      CUSTOM_SCHEDULED_TASK.TASK_DAY, 
      CUSTOM_SCHEDULED_TASK.TRIGGER_TIME, 
      CUSTOM_SCHEDULED_TASK.SCHEDULE_GROUPING, 
      CUSTOM_SCHEDULED_TASK.FACILITY_ID
   FROM SBMO.CUSTOM_SCHEDULED_TASK
   WHERE CUSTOM_SCHEDULED_TASK.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.CUSTOM_SCHEDULED_TASK',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'CUSTOM_SCHEDULED_TASK'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'DAILY_SCHEDULED_TASKS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[DAILY_SCHEDULED_TASKS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.DAILY_SCHEDULED_TASKS (TENANT_ID, ID, FACILITY_ID)
AS 
   SELECT DAILY_SCHEDULED_TASKS.TENANT_ID, DAILY_SCHEDULED_TASKS.ID, DAILY_SCHEDULED_TASKS.FACILITY_ID
   FROM SBMO.DAILY_SCHEDULED_TASKS
   WHERE DAILY_SCHEDULED_TASKS.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.DAILY_SCHEDULED_TASKS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'DAILY_SCHEDULED_TASKS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'DISPENSER_LOCATION' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[DISPENSER_LOCATION]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.DISPENSER_LOCATION (
   TENANT_ID, 
   ID, 
   DISPENSER_ID, 
   LOCATION_ID, 
   CELLID, 
   STATIONID)
AS 
   SELECT 
      DISPENSER_LOCATION.TENANT_ID, 
      DISPENSER_LOCATION.ID, 
      DISPENSER_LOCATION.DISPENSER_ID, 
      DISPENSER_LOCATION.LOCATION_ID, 
      DISPENSER_LOCATION.CELLID, 
      DISPENSER_LOCATION.STATIONID
   FROM SBMO.DISPENSER_LOCATION
   WHERE DISPENSER_LOCATION.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.DISPENSER_LOCATION',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'DISPENSER_LOCATION'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'DISPENSERS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[DISPENSERS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.DISPENSERS (
   TENANT_ID, 
   ID, 
   WARNING_LABEL_CODE, 
   LOAD_BALANCE_FACTOR, 
   MODULE, 
   PORT, 
   TIMEOUT, 
   HOST, 
   VERSION, 
   DISABLED, 
   INVENTORY_ID, 
   TRANSPORT_PROPERTIES, 
   USES_BATCH_IDENTIFIER, 
   DESCRIPTION, 
   FACILITY_ID, 
   DISPENSER_CERT_SUBJECT, 
   ORDER_PULL_RETRY_COUNT, 
   LOG_DISPENSER_REQUEST, 
   LAST_UPDATED_DATE, 
   LAST_UPDATED_USER_ID)
AS 
   SELECT 
      DISPENSERS.TENANT_ID, 
      DISPENSERS.ID, 
      DISPENSERS.WARNING_LABEL_CODE, 
      DISPENSERS.LOAD_BALANCE_FACTOR, 
      DISPENSERS.MODULE, 
      DISPENSERS.PORT, 
      DISPENSERS.TIMEOUT, 
      DISPENSERS.HOST, 
      DISPENSERS.VERSION, 
      DISPENSERS.DISABLED, 
      DISPENSERS.INVENTORY_ID, 
      DISPENSERS.TRANSPORT_PROPERTIES, 
      DISPENSERS.USES_BATCH_IDENTIFIER, 
      DISPENSERS.DESCRIPTION, 
      DISPENSERS.FACILITY_ID, 
      DISPENSERS.DISPENSER_CERT_SUBJECT, 
      DISPENSERS.ORDER_PULL_RETRY_COUNT, 
      DISPENSERS.LOG_DISPENSER_REQUEST, 
      DISPENSERS.LAST_UPDATED_DATE, 
      DISPENSERS.LAST_UPDATED_USER_ID
   FROM SBMO.DISPENSERS
   WHERE DISPENSERS.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.DISPENSERS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'DISPENSERS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'DRUG_ORDER_PRODUCT_VERIFY' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[DRUG_ORDER_PRODUCT_VERIFY]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.DRUG_ORDER_PRODUCT_VERIFY (
   TENANT_ID, 
   ID, 
   DRUG_ORDERS_ID, 
   MASTER_PRODUCT_PO_VERIFY_ID, 
   ITEM_PASSED, 
   NDC, 
   GTIN, 
   EXPIRATION_DATE, 
   SERIAL_NUMBER, 
   LOT_NUMBER, 
   NOTES, 
   DATE_SCANNED, 
   BAR_LINE, 
   SCANNED_BY_USER, 
   CREATED_DATE)
AS 
   SELECT 
      DRUG_ORDER_PRODUCT_VERIFY.TENANT_ID, 
      DRUG_ORDER_PRODUCT_VERIFY.ID, 
      DRUG_ORDER_PRODUCT_VERIFY.DRUG_ORDERS_ID, 
      DRUG_ORDER_PRODUCT_VERIFY.MASTER_PRODUCT_PO_VERIFY_ID, 
      DRUG_ORDER_PRODUCT_VERIFY.ITEM_PASSED, 
      DRUG_ORDER_PRODUCT_VERIFY.NDC, 
      DRUG_ORDER_PRODUCT_VERIFY.GTIN, 
      DRUG_ORDER_PRODUCT_VERIFY.EXPIRATION_DATE, 
      DRUG_ORDER_PRODUCT_VERIFY.SERIAL_NUMBER, 
      DRUG_ORDER_PRODUCT_VERIFY.LOT_NUMBER, 
      DRUG_ORDER_PRODUCT_VERIFY.NOTES, 
      DRUG_ORDER_PRODUCT_VERIFY.DATE_SCANNED, 
      DRUG_ORDER_PRODUCT_VERIFY.BAR_LINE, 
      DRUG_ORDER_PRODUCT_VERIFY.SCANNED_BY_USER, 
      DRUG_ORDER_PRODUCT_VERIFY.CREATED_DATE
   FROM SBMO.DRUG_ORDER_PRODUCT_VERIFY
   WHERE DRUG_ORDER_PRODUCT_VERIFY.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.DRUG_ORDER_PRODUCT_VERIFY',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'DRUG_ORDER_PRODUCT_VERIFY'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'DRUG_ORDERS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[DRUG_ORDERS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.DRUG_ORDERS (
   TENANT_ID, 
   ID, 
   PARTITION_STAMP, 
   NDC, 
   ACK_RECEIVED_QTY, 
   PACK, 
   COST, 
   COUNTER, 
   ITEM_STATUS, 
   SUB_ITEM_NUMBER, 
   ITEM_NUMBER, 
   ORDER_QTY, 
   RECQTY, 
   CHECK_IN_DATE, 
   APPLIED_TO_INVENTORY_DATE, 
   FORMULARY_ID, 
   PURCHASE_ORDER_ID, 
   VENDOR_ORDER_QTY, 
   VENDOR_ACK_RECEIVED_QTY, 
   VENDOR_RECQTY, 
   APPLIED_QTY, 
   CREATED_DATE, 
   CONVERSION_FACTOR, 
   VENDOR_ORDER_CASE)
AS 
   SELECT 
      DRUG_ORDERS.TENANT_ID, 
      DRUG_ORDERS.ID, 
      DRUG_ORDERS.PARTITION_STAMP, 
      DRUG_ORDERS.NDC, 
      DRUG_ORDERS.ACK_RECEIVED_QTY, 
      DRUG_ORDERS.PACK, 
      DRUG_ORDERS.COST, 
      DRUG_ORDERS.COUNTER, 
      DRUG_ORDERS.ITEM_STATUS, 
      DRUG_ORDERS.SUB_ITEM_NUMBER, 
      DRUG_ORDERS.ITEM_NUMBER, 
      DRUG_ORDERS.ORDER_QTY, 
      DRUG_ORDERS.RECQTY, 
      DRUG_ORDERS.CHECK_IN_DATE, 
      DRUG_ORDERS.APPLIED_TO_INVENTORY_DATE, 
      DRUG_ORDERS.FORMULARY_ID, 
      DRUG_ORDERS.PURCHASE_ORDER_ID, 
      DRUG_ORDERS.VENDOR_ORDER_QTY, 
      DRUG_ORDERS.VENDOR_ACK_RECEIVED_QTY, 
      DRUG_ORDERS.VENDOR_RECQTY, 
      DRUG_ORDERS.APPLIED_QTY, 
      DRUG_ORDERS.CREATED_DATE, 
      DRUG_ORDERS.CONVERSION_FACTOR, 
      DRUG_ORDERS.VENDOR_ORDER_CASE
   FROM SBMO.DRUG_ORDERS
   WHERE DRUG_ORDERS.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.DRUG_ORDERS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'DRUG_ORDERS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'DRUGS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[DRUGS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.DRUGS (
   ID, 
   DECIMAL_PACK, 
   GENERIC_FULL_NAME, 
   DRUG_FULL_NAME, 
   GENERIC_NAME, 
   GPI, 
   INJECT, 
   DRUG_NOTE, 
   MFG, 
   NAME, 
   NDC, 
   PACK, 
   PREVIOUS_NDC, 
   REPLACEMENT_NDC, 
   SCHEDULE, 
   STRENGTH, 
   TRIPLIC, 
   UNIT, 
   MFG_DISCONTINUE_DATE, 
   LAST_UPDATED_DATE, 
   SIGNATURE_REQUIRED, 
   AWP, 
   AWP_LAST_UPDATED, 
   BARCODE, 
   NDCFMT, 
   PACKAGE_QTY, 
   IND_PACK, 
   MULTI_SOURCE, 
   DDID, 
   NARCOTIC_INDICATOR, 
   DEEP_FROZEN, 
   BIN_STORAGE_TYPE, 
   HAZARDOUS_DRUG, 
   INNER_PACK_INDICATOR)
AS 
   SELECT 
      DRUGS.ID, 
      DRUGS.DECIMAL_PACK, 
      DRUGS.GENERIC_FULL_NAME, 
      DRUGS.DRUG_FULL_NAME, 
      DRUGS.GENERIC_NAME, 
      DRUGS.GPI, 
      DRUGS.INJECT, 
      DRUGS.DRUG_NOTE, 
      DRUGS.MFG, 
      DRUGS.NAME, 
      DRUGS.NDC, 
      DRUGS.PACK, 
      DRUGS.PREVIOUS_NDC, 
      DRUGS.REPLACEMENT_NDC, 
      DRUGS.SCHEDULE, 
      DRUGS.STRENGTH, 
      DRUGS.TRIPLIC, 
      DRUGS.UNIT, 
      DRUGS.MFG_DISCONTINUE_DATE, 
      DRUGS.LAST_UPDATED_DATE, 
      DRUGS.SIGNATURE_REQUIRED, 
      DRUGS.AWP, 
      DRUGS.AWP_LAST_UPDATED, 
      DRUGS.BARCODE, 
      DRUGS.NDCFMT, 
      DRUGS.PACKAGE_QTY, 
      DRUGS.IND_PACK, 
      DRUGS.MULTI_SOURCE, 
      DRUGS.DDID, 
      DRUGS.NARCOTIC_INDICATOR, 
      DRUGS.DEEP_FROZEN, 
      DRUGS.BIN_STORAGE_TYPE, 
      DRUGS.HAZARDOUS_DRUG, 
      DRUGS.INNER_PACK_INDICATOR
   FROM SBMO.DRUGS
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.DRUGS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'DRUGS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'ENTITIES' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[ENTITIES]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.ENTITIES (TENANT_ID, ID)
AS 
   SELECT ENTITIES.TENANT_ID, ENTITIES.ID
   FROM SBMO.ENTITIES
   WHERE ENTITIES.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.ENTITIES',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'ENTITIES'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'FACILITIES' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[FACILITIES]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.FACILITIES (
   TENANT_ID, 
   ID, 
   SHORT_NAME, 
   NAME, 
   ADDRESS_LINE_1, 
   ADDRESS_LINE_2, 
   CITY, 
   STATE, 
   ZIP, 
   COUNTY, 
   DEA_NUMBER, 
   FEDERAL_TAX_IDENTIFIER, 
   NCPDP_NUMBER, 
   NHINID, 
   DEA_EXPIRATION_DATE, 
   TIME_ZONE, 
   LOCAL_PHONE_NUMBER, 
   TOLL_FREE_PHONE_NUMBER, 
   ALTERNATE_PHONE_NUMBER, 
   EXPORT_ORDER_DISPENSER_MODULE, 
   DISPENSER_COMM_ENABLED, 
   CHECK_INV_FOR_HOME_DELIVERY, 
   DRUG_LOT_NOT_REQUIRED, 
   ORDER_PULL_DELAY, 
   ENABLE_ITEM_LOCATIONS, 
   RED_HOURS, 
   YELLOW_HOURS, 
   ACTIVE_DIRECTORY_GROUP, 
   PAUSE_ORDER_PROCESSING_DATE, 
   DOC_ERROR_RESET_DATE, 
   DOC_CON_ERROR_RESET_DATE, 
   DISPLAY_TX_COST_ERROR, 
   LANDING_PAGE, 
   STATUS_ALERT_THRESHOLD, 
   PROMISED_DATE_ALERT_THRESHOLD, 
   ORDER_PULL_JOB_FREQUENCY, 
   CHECK_INV_FOR_SPECIALTY, 
   TRANSITION_POLLTIME, 
   TRANSITION_ALERT_THRESHOLD, 
   TRANSITION_AUTO_TASK_DISABLED, 
   TRANSITION_ALERT_EMAIL, 
   PROMISE_DATE_ENABLED, 
   PROMISE_DATE_SUN, 
   PROMISE_DATE_MON, 
   PROMISE_DATE_TUE, 
   PROMISE_DATE_WED, 
   PROMISE_DATE_THU, 
   PROMISE_DATE_FRI, 
   PROMISE_DATE_SAT, 
   AUTO_CANCEL_TO_STORE, 
   QUEUE_MAX_DAYS_BACK, 
   CHECK_INV_FOR_STORE_DELIVERY, 
   MAINTAIN_INVENTORY_QUANTITIES, 
   PO_EMAIL, 
   ORDER_PULL_THREAD_CNT, 
   ORDER_PULL_MAX_CNT, 
   ENABLE_HOLD_SPECIALTY_ORDERS, 
   LAST_UPDATED_DATE, 
   LAST_UPDATED_USER_ID, 
   ENABLE_ORDER_CANCEL_REASON, 
   DASHBOARD_JOB_ENABLED, 
   STATUS_UPDATE_MAX_DAYS_BACK, 
   IGNORE_PROM_DATE_HOME_DELIVERY, 
   IGNORE_PROM_DATE_SPECIALTY, 
   CHECK_INV_FOR_UNIT_DOSE, 
   START_OF_DAY_TIME, 
   ADHERIS_DOCUMENT_SIZE)
AS 
   SELECT 
      FACILITIES.TENANT_ID, 
      FACILITIES.ID, 
      FACILITIES.SHORT_NAME, 
      FACILITIES.NAME, 
      FACILITIES.ADDRESS_LINE_1, 
      FACILITIES.ADDRESS_LINE_2, 
      FACILITIES.CITY, 
      FACILITIES.STATE, 
      FACILITIES.ZIP, 
      FACILITIES.COUNTY, 
      FACILITIES.DEA_NUMBER, 
      FACILITIES.FEDERAL_TAX_IDENTIFIER, 
      FACILITIES.NCPDP_NUMBER, 
      FACILITIES.NHINID, 
      FACILITIES.DEA_EXPIRATION_DATE, 
      FACILITIES.TIME_ZONE, 
      FACILITIES.LOCAL_PHONE_NUMBER, 
      FACILITIES.TOLL_FREE_PHONE_NUMBER, 
      FACILITIES.ALTERNATE_PHONE_NUMBER, 
      FACILITIES.EXPORT_ORDER_DISPENSER_MODULE, 
      FACILITIES.DISPENSER_COMM_ENABLED, 
      FACILITIES.CHECK_INV_FOR_HOME_DELIVERY, 
      FACILITIES.DRUG_LOT_NOT_REQUIRED, 
      FACILITIES.ORDER_PULL_DELAY, 
      FACILITIES.ENABLE_ITEM_LOCATIONS, 
      FACILITIES.RED_HOURS, 
      FACILITIES.YELLOW_HOURS, 
      FACILITIES.ACTIVE_DIRECTORY_GROUP, 
      FACILITIES.PAUSE_ORDER_PROCESSING_DATE, 
      FACILITIES.DOC_ERROR_RESET_DATE, 
      FACILITIES.DOC_CON_ERROR_RESET_DATE, 
      FACILITIES.DISPLAY_TX_COST_ERROR, 
      FACILITIES.LANDING_PAGE, 
      FACILITIES.STATUS_ALERT_THRESHOLD, 
      FACILITIES.PROMISED_DATE_ALERT_THRESHOLD, 
      FACILITIES.ORDER_PULL_JOB_FREQUENCY, 
      FACILITIES.CHECK_INV_FOR_SPECIALTY, 
      FACILITIES.TRANSITION_POLLTIME, 
      FACILITIES.TRANSITION_ALERT_THRESHOLD, 
      FACILITIES.TRANSITION_AUTO_TASK_DISABLED, 
      FACILITIES.TRANSITION_ALERT_EMAIL, 
      FACILITIES.PROMISE_DATE_ENABLED, 
      FACILITIES.PROMISE_DATE_SUN, 
      FACILITIES.PROMISE_DATE_MON, 
      FACILITIES.PROMISE_DATE_TUE, 
      FACILITIES.PROMISE_DATE_WED, 
      FACILITIES.PROMISE_DATE_THU, 
      FACILITIES.PROMISE_DATE_FRI, 
      FACILITIES.PROMISE_DATE_SAT, 
      FACILITIES.AUTO_CANCEL_TO_STORE, 
      FACILITIES.QUEUE_MAX_DAYS_BACK, 
      FACILITIES.CHECK_INV_FOR_STORE_DELIVERY, 
      FACILITIES.MAINTAIN_INVENTORY_QUANTITIES, 
      FACILITIES.PO_EMAIL, 
      FACILITIES.ORDER_PULL_THREAD_CNT, 
      FACILITIES.ORDER_PULL_MAX_CNT, 
      FACILITIES.ENABLE_HOLD_SPECIALTY_ORDERS, 
      FACILITIES.LAST_UPDATED_DATE, 
      FACILITIES.LAST_UPDATED_USER_ID, 
      FACILITIES.ENABLE_ORDER_CANCEL_REASON, 
      FACILITIES.DASHBOARD_JOB_ENABLED, 
      FACILITIES.STATUS_UPDATE_MAX_DAYS_BACK, 
      FACILITIES.IGNORE_PROM_DATE_HOME_DELIVERY, 
      FACILITIES.IGNORE_PROM_DATE_SPECIALTY, 
      FACILITIES.CHECK_INV_FOR_UNIT_DOSE, 
      FACILITIES.START_OF_DAY_TIME, 
      FACILITIES.ADHERIS_DOCUMENT_SIZE
   FROM SBMO.FACILITIES
   WHERE FACILITIES.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.FACILITIES',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'FACILITIES'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'FORMULARY' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[FORMULARY]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.FORMULARY (
   TENANT_ID, 
   ID, 
   DEACTIVATED_DATE, 
   EFFECTIVE_DATE, 
   CONSIGNED, 
   LAST_UPDATED_DATE, 
   NDC, 
   ALLOCATED_QTY, 
   ONHAND_QTY, 
   DRUG_ID, 
   VENDOR_ID, 
   INVENTORY_ID, 
   LAST_DISPENSED_DATE, 
   AVAILABLE_FOR_TYPE, 
   ONHAND_ORIG_QTY, 
   OVERRIDE_SHIP_ID, 
   PRIORITY, 
   OVERRIDE_INVENTORY, 
   FILLING_STATION_ID, 
   DISPENSED_INCREMENT, 
   NOTE)
AS 
   SELECT 
      FORMULARY.TENANT_ID, 
      FORMULARY.ID, 
      FORMULARY.DEACTIVATED_DATE, 
      FORMULARY.EFFECTIVE_DATE, 
      FORMULARY.CONSIGNED, 
      FORMULARY.LAST_UPDATED_DATE, 
      FORMULARY.NDC, 
      FORMULARY.ALLOCATED_QTY, 
      FORMULARY.ONHAND_QTY, 
      FORMULARY.DRUG_ID, 
      FORMULARY.VENDOR_ID, 
      FORMULARY.INVENTORY_ID, 
      FORMULARY.LAST_DISPENSED_DATE, 
      FORMULARY.AVAILABLE_FOR_TYPE, 
      FORMULARY.ONHAND_ORIG_QTY, 
      FORMULARY.OVERRIDE_SHIP_ID, 
      FORMULARY.PRIORITY, 
      FORMULARY.OVERRIDE_INVENTORY, 
      FORMULARY.FILLING_STATION_ID, 
      FORMULARY.DISPENSED_INCREMENT, 
      FORMULARY.NOTE
   FROM SBMO.FORMULARY
   WHERE FORMULARY.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.FORMULARY',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'FORMULARY'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'FORMULARY_REORDER' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[FORMULARY_REORDER]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.FORMULARY_REORDER (
   TENANT_ID, 
   ID, 
   ORDER_POINT, 
   ORDER_QTY, 
   MINIMUM_QTY, 
   LAST_ORDER_DATE, 
   HOLD_UNTIL_DATE)
AS 
   SELECT 
      FORMULARY_REORDER.TENANT_ID, 
      FORMULARY_REORDER.ID, 
      FORMULARY_REORDER.ORDER_POINT, 
      FORMULARY_REORDER.ORDER_QTY, 
      FORMULARY_REORDER.MINIMUM_QTY, 
      FORMULARY_REORDER.LAST_ORDER_DATE, 
      FORMULARY_REORDER.HOLD_UNTIL_DATE
   FROM SBMO.FORMULARY_REORDER
   WHERE FORMULARY_REORDER.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.FORMULARY_REORDER',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'FORMULARY_REORDER'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'INVENTORY' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[INVENTORY]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.INVENTORY (
   TENANT_ID, 
   ID, 
   NAME, 
   DATE_OF_LAST_PHYSICAL_COUNT, 
   FACILITY_ID, 
   AVAILABLE_FOR_TYPE, 
   IGNORE_NDC_FORMULARY_STORE_DEL, 
   IGNORE_NDC_FORMULARY_MAIL_DEL, 
   IGNORE_NDC_FORMULARY_SPECIALTY)
AS 
   SELECT 
      INVENTORY.TENANT_ID, 
      INVENTORY.ID, 
      INVENTORY.NAME, 
      INVENTORY.DATE_OF_LAST_PHYSICAL_COUNT, 
      INVENTORY.FACILITY_ID, 
      INVENTORY.AVAILABLE_FOR_TYPE, 
      INVENTORY.IGNORE_NDC_FORMULARY_STORE_DEL, 
      INVENTORY.IGNORE_NDC_FORMULARY_MAIL_DEL, 
      INVENTORY.IGNORE_NDC_FORMULARY_SPECIALTY
   FROM SBMO.INVENTORY
   WHERE INVENTORY.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.INVENTORY',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'INVENTORY'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'INVENTORY_AUDIT' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[INVENTORY_AUDIT]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.INVENTORY_AUDIT (
   TENANT_ID, 
   ID, 
   ALLOCATED_CHANGE, 
   ONHAND_CHANGE, 
   UPDATED_DATE, 
   DRUG_ORDER_ID, 
   FORMULARY_ID, 
   RET_ADJ_ID, 
   RX_ID, 
   CREATED_DATE)
AS 
   SELECT 
      INVENTORY_AUDIT.TENANT_ID, 
      INVENTORY_AUDIT.ID, 
      INVENTORY_AUDIT.ALLOCATED_CHANGE, 
      INVENTORY_AUDIT.ONHAND_CHANGE, 
      INVENTORY_AUDIT.UPDATED_DATE, 
      INVENTORY_AUDIT.DRUG_ORDER_ID, 
      INVENTORY_AUDIT.FORMULARY_ID, 
      INVENTORY_AUDIT.RET_ADJ_ID, 
      INVENTORY_AUDIT.RX_ID, 
      INVENTORY_AUDIT.CREATED_DATE
   FROM SBMO.INVENTORY_AUDIT
   WHERE INVENTORY_AUDIT.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.INVENTORY_AUDIT',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'INVENTORY_AUDIT'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'INVENTORY_DATE' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[INVENTORY_DATE]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.INVENTORY_DATE (TENANT_ID, ID, INVENTORY_LAST_UPDATED_DATE)
AS 
   SELECT INVENTORY_DATE.TENANT_ID, INVENTORY_DATE.ID, INVENTORY_DATE.INVENTORY_LAST_UPDATED_DATE
   FROM SBMO.INVENTORY_DATE
   WHERE INVENTORY_DATE.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.INVENTORY_DATE',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'INVENTORY_DATE'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'ITEM_LOCATION' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[ITEM_LOCATION]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.ITEM_LOCATION (
   TENANT_ID, 
   ID, 
   FORMULARY_ID, 
   QUANTITY)
AS 
   SELECT ITEM_LOCATION.TENANT_ID, ITEM_LOCATION.ID, ITEM_LOCATION.FORMULARY_ID, ITEM_LOCATION.QUANTITY
   FROM SBMO.ITEM_LOCATION
   WHERE ITEM_LOCATION.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.ITEM_LOCATION',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'ITEM_LOCATION'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'LABEL_IMAGES' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[LABEL_IMAGES]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.LABEL_IMAGES (
   TENANT_ID, 
   ID, 
   IMAGE_DATA, 
   IMAGE_FORMAT, 
   IMAGE_NAME, 
   IMAGE_SOURCE)
AS 
   SELECT 
      LABEL_IMAGES.TENANT_ID, 
      LABEL_IMAGES.ID, 
      LABEL_IMAGES.IMAGE_DATA, 
      LABEL_IMAGES.IMAGE_FORMAT, 
      LABEL_IMAGES.IMAGE_NAME, 
      LABEL_IMAGES.IMAGE_SOURCE
   FROM SBMO.LABEL_IMAGES
   WHERE LABEL_IMAGES.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.LABEL_IMAGES',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'LABEL_IMAGES'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'LOCATION' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[LOCATION]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.LOCATION (
   TENANT_ID, 
   ID, 
   LOCATION_TYPE, 
   PARENT_ID, 
   USE_TYPE, 
   DESCRIPTOR, 
   LOCATION_STATUS)
AS 
   SELECT 
      LOCATION.TENANT_ID, 
      LOCATION.ID, 
      LOCATION.LOCATION_TYPE, 
      LOCATION.PARENT_ID, 
      LOCATION.USE_TYPE, 
      LOCATION.DESCRIPTOR, 
      LOCATION.LOCATION_STATUS
   FROM SBMO.LOCATION
   WHERE LOCATION.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.LOCATION',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'LOCATION'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'MASTER_PRODUCT_PO_VERIFY' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[MASTER_PRODUCT_PO_VERIFY]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.MASTER_PRODUCT_PO_VERIFY (
   TENANT_ID, 
   ID, 
   PURCHASE_ORDER_ID, 
   VENDOR_ID, 
   ORDER_DATE, 
   TOTAL_ITEMS, 
   PERCENT_SCANNED, 
   PERCENT_PASSED, 
   CREATED_DATE, 
   PURCHASE_ORDER_STATUS)
AS 
   SELECT 
      MASTER_PRODUCT_PO_VERIFY.TENANT_ID, 
      MASTER_PRODUCT_PO_VERIFY.ID, 
      MASTER_PRODUCT_PO_VERIFY.PURCHASE_ORDER_ID, 
      MASTER_PRODUCT_PO_VERIFY.VENDOR_ID, 
      MASTER_PRODUCT_PO_VERIFY.ORDER_DATE, 
      MASTER_PRODUCT_PO_VERIFY.TOTAL_ITEMS, 
      MASTER_PRODUCT_PO_VERIFY.PERCENT_SCANNED, 
      MASTER_PRODUCT_PO_VERIFY.PERCENT_PASSED, 
      MASTER_PRODUCT_PO_VERIFY.CREATED_DATE, 
      MASTER_PRODUCT_PO_VERIFY.PURCHASE_ORDER_STATUS
   FROM SBMO.MASTER_PRODUCT_PO_VERIFY
   WHERE MASTER_PRODUCT_PO_VERIFY.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.MASTER_PRODUCT_PO_VERIFY',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'MASTER_PRODUCT_PO_VERIFY'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'MESSAGE_LOG' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[MESSAGE_LOG]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.MESSAGE_LOG (
   TENANT_ID, 
   ID, 
   MESSAGE_DATE, 
   MESSAGE, 
   STORE_ID, 
   USER_INITS, 
   CREATED_DATE)
AS 
   SELECT 
      MESSAGE_LOG.TENANT_ID, 
      MESSAGE_LOG.ID, 
      MESSAGE_LOG.MESSAGE_DATE, 
      MESSAGE_LOG.MESSAGE, 
      MESSAGE_LOG.STORE_ID, 
      MESSAGE_LOG.USER_INITS, 
      MESSAGE_LOG.CREATED_DATE
   FROM SBMO.MESSAGE_LOG
   WHERE MESSAGE_LOG.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.MESSAGE_LOG',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'MESSAGE_LOG'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'MONTHLY_SCHEDULED_TASKS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[MONTHLY_SCHEDULED_TASKS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.MONTHLY_SCHEDULED_TASKS (
   TENANT_ID, 
   ID, 
   APRIL, 
   AUGUST, 
   DECEMBER, 
   FEBRUARY, 
   JANUARY, 
   JULY, 
   JUNE, 
   MARCH, 
   MAY, 
   NOVEMBER, 
   OCTOBER, 
   SEPTEMBER, 
   TYPE, 
   DATE_OF_MONTH, 
   DAY_OF_MONTH, 
   FIRST_LAST, 
   DAY_TYPE, 
   FACILITY_ID)
AS 
   SELECT 
      MONTHLY_SCHEDULED_TASKS.TENANT_ID, 
      MONTHLY_SCHEDULED_TASKS.ID, 
      MONTHLY_SCHEDULED_TASKS.APRIL, 
      MONTHLY_SCHEDULED_TASKS.AUGUST, 
      MONTHLY_SCHEDULED_TASKS.DECEMBER, 
      MONTHLY_SCHEDULED_TASKS.FEBRUARY, 
      MONTHLY_SCHEDULED_TASKS.JANUARY, 
      MONTHLY_SCHEDULED_TASKS.JULY, 
      MONTHLY_SCHEDULED_TASKS.JUNE, 
      MONTHLY_SCHEDULED_TASKS.MARCH, 
      MONTHLY_SCHEDULED_TASKS.MAY, 
      MONTHLY_SCHEDULED_TASKS.NOVEMBER, 
      MONTHLY_SCHEDULED_TASKS.OCTOBER, 
      MONTHLY_SCHEDULED_TASKS.SEPTEMBER, 
      MONTHLY_SCHEDULED_TASKS.TYPE, 
      MONTHLY_SCHEDULED_TASKS.DATE_OF_MONTH, 
      MONTHLY_SCHEDULED_TASKS.DAY_OF_MONTH, 
      MONTHLY_SCHEDULED_TASKS.FIRST_LAST, 
      MONTHLY_SCHEDULED_TASKS.DAY_TYPE, 
      MONTHLY_SCHEDULED_TASKS.FACILITY_ID
   FROM SBMO.MONTHLY_SCHEDULED_TASKS
   WHERE MONTHLY_SCHEDULED_TASKS.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.MONTHLY_SCHEDULED_TASKS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'MONTHLY_SCHEDULED_TASKS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'ONCE_SCHEDULED_TASKS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[ONCE_SCHEDULED_TASKS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.ONCE_SCHEDULED_TASKS (
   TENANT_ID, 
   ID, 
   START_DATE, 
   FACILITY_ID)
AS 
   SELECT ONCE_SCHEDULED_TASKS.TENANT_ID, ONCE_SCHEDULED_TASKS.ID, ONCE_SCHEDULED_TASKS.START_DATE, ONCE_SCHEDULED_TASKS.FACILITY_ID
   FROM SBMO.ONCE_SCHEDULED_TASKS
   WHERE ONCE_SCHEDULED_TASKS.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.ONCE_SCHEDULED_TASKS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'ONCE_SCHEDULED_TASKS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'ORDERS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[ORDERS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.ORDERS (
   TENANT_ID, 
   ID, 
   PARTITION_STAMP, 
   ORDER_NUMBER, 
   SHIP_ADDRESS, 
   SHIP_ADDRESS2, 
   SHIP_CITY, 
   SHIP_FIRST_NAME, 
   SHIP_MIDDLE_NAME, 
   SHIP_LAST_NAME, 
   SHIP_PHONE, 
   SHIP_STATE, 
   ORDER_TYPE, 
   SHIP_TYPE, 
   SHIP_ZIP, 
   STORE_DEA, 
   STORE_LIC, 
   STORE_EST_SHIP_AMT, 
   SIGNATURE_REQUIRED, 
   STORE_ID, 
   ORDER_PROCESSED, 
   INVENTORY_ID, 
   GROUPING_CODE, 
   ALLOW_GROUPING, 
   BATCH_IDENTIFIER, 
   CREATED_DATE, 
   PROMISED_DATE, 
   PROMISE_DATE_OVERRIDE, 
   REQUESTED_SHIP_DATE, 
   SHIPPING_INSTRUCTIONS, 
   EXTERNAL_ORDER_IDENTIFIER, 
   VENDOR_CARRIER_CODE_IDENT, 
   VENDOR_CARRIER_CODE_DESC, 
   VENDOR_SHIP_BOX_IDENT)
AS 
   SELECT 
      ORDERS.TENANT_ID, 
      ORDERS.ID, 
      ORDERS.PARTITION_STAMP, 
      ORDERS.ORDER_NUMBER, 
      ORDERS.SHIP_ADDRESS, 
      ORDERS.SHIP_ADDRESS2, 
      ORDERS.SHIP_CITY, 
      ORDERS.SHIP_FIRST_NAME, 
      ORDERS.SHIP_MIDDLE_NAME, 
      ORDERS.SHIP_LAST_NAME, 
      ORDERS.SHIP_PHONE, 
      ORDERS.SHIP_STATE, 
      ORDERS.ORDER_TYPE, 
      ORDERS.SHIP_TYPE, 
      ORDERS.SHIP_ZIP, 
      ORDERS.STORE_DEA, 
      ORDERS.STORE_LIC, 
      ORDERS.STORE_EST_SHIP_AMT, 
      ORDERS.SIGNATURE_REQUIRED, 
      ORDERS.STORE_ID, 
      ORDERS.ORDER_PROCESSED, 
      ORDERS.INVENTORY_ID, 
      ORDERS.GROUPING_CODE, 
      ORDERS.ALLOW_GROUPING, 
      ORDERS.BATCH_IDENTIFIER, 
      ORDERS.CREATED_DATE, 
      ORDERS.PROMISED_DATE, 
      ORDERS.PROMISE_DATE_OVERRIDE, 
      ORDERS.REQUESTED_SHIP_DATE, 
      ORDERS.SHIPPING_INSTRUCTIONS, 
      ORDERS.EXTERNAL_ORDER_IDENTIFIER, 
      ORDERS.VENDOR_CARRIER_CODE_IDENT, 
      ORDERS.VENDOR_CARRIER_CODE_DESC, 
      ORDERS.VENDOR_SHIP_BOX_IDENT
   FROM SBMO.ORDERS
   WHERE ORDERS.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.ORDERS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'ORDERS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'PHONE_ADDRESS_OWNERS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[PHONE_ADDRESS_OWNERS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.PHONE_ADDRESS_OWNERS (TENANT_ID, ID)
AS 
   SELECT PHONE_ADDRESS_OWNERS.TENANT_ID, PHONE_ADDRESS_OWNERS.ID
   FROM SBMO.PHONE_ADDRESS_OWNERS
   WHERE PHONE_ADDRESS_OWNERS.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.PHONE_ADDRESS_OWNERS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'PHONE_ADDRESS_OWNERS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'PHONES' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[PHONES]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.PHONES (
   TENANT_ID, 
   ID, 
   PHONE_NUMBER, 
   OWNER_ID, 
   IDX)
AS 
   SELECT 
      PHONES.TENANT_ID, 
      PHONES.ID, 
      PHONES.PHONE_NUMBER, 
      PHONES.OWNER_ID, 
      PHONES.IDX
   FROM SBMO.PHONES
   WHERE PHONES.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.PHONES',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'PHONES'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'PRIORITY_CHANGE_ALERTS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[PRIORITY_CHANGE_ALERTS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.PRIORITY_CHANGE_ALERTS (
   TENANT_ID, 
   ID, 
   FORMULARY_ID, 
   NDC, 
   ACTION, 
   ADDED_DATE, 
   ACKNOWLEDGED, 
   ACKNOWLEDGED_BY, 
   FACILITY_ID)
AS 
   SELECT 
      PRIORITY_CHANGE_ALERTS.TENANT_ID, 
      PRIORITY_CHANGE_ALERTS.ID, 
      PRIORITY_CHANGE_ALERTS.FORMULARY_ID, 
      PRIORITY_CHANGE_ALERTS.NDC, 
      PRIORITY_CHANGE_ALERTS.ACTION, 
      PRIORITY_CHANGE_ALERTS.ADDED_DATE, 
      PRIORITY_CHANGE_ALERTS.ACKNOWLEDGED, 
      PRIORITY_CHANGE_ALERTS.ACKNOWLEDGED_BY, 
      PRIORITY_CHANGE_ALERTS.FACILITY_ID
   FROM SBMO.PRIORITY_CHANGE_ALERTS
   WHERE PRIORITY_CHANGE_ALERTS.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.PRIORITY_CHANGE_ALERTS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'PRIORITY_CHANGE_ALERTS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'PURCHASE_ORDER' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[PURCHASE_ORDER]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.PURCHASE_ORDER (
   TENANT_ID, 
   ID, 
   PARTITION_STAMP, 
   PO_GROUP_ID, 
   CONSIGNED, 
   VENDOR_ID, 
   PURCHASE_ORDER_NUMBER, 
   ACK_RECEIVED_USER_ID, 
   CREATED_USER_ID, 
   APPLIED_TO_INVENTORY_DATE, 
   ACK_RECEIVED_DATE, 
   APPLIED_TO_INVENTORY_USER_ID, 
   ORDER_TRANSMIT_DATE, 
   ORDER_TRANSMIT_USER_ID, 
   CREATED_DATE, 
   CHECK_IN_DATE, 
   SUBMIT_DATE, 
   CONTROL_NUMBER, 
   INVOICE_NUMBER)
AS 
   SELECT 
      PURCHASE_ORDER.TENANT_ID, 
      PURCHASE_ORDER.ID, 
      PURCHASE_ORDER.PARTITION_STAMP, 
      PURCHASE_ORDER.PO_GROUP_ID, 
      PURCHASE_ORDER.CONSIGNED, 
      PURCHASE_ORDER.VENDOR_ID, 
      PURCHASE_ORDER.PURCHASE_ORDER_NUMBER, 
      PURCHASE_ORDER.ACK_RECEIVED_USER_ID, 
      PURCHASE_ORDER.CREATED_USER_ID, 
      PURCHASE_ORDER.APPLIED_TO_INVENTORY_DATE, 
      PURCHASE_ORDER.ACK_RECEIVED_DATE, 
      PURCHASE_ORDER.APPLIED_TO_INVENTORY_USER_ID, 
      PURCHASE_ORDER.ORDER_TRANSMIT_DATE, 
      PURCHASE_ORDER.ORDER_TRANSMIT_USER_ID, 
      PURCHASE_ORDER.CREATED_DATE, 
      PURCHASE_ORDER.CHECK_IN_DATE, 
      PURCHASE_ORDER.SUBMIT_DATE, 
      PURCHASE_ORDER.CONTROL_NUMBER, 
      PURCHASE_ORDER.INVOICE_NUMBER
   FROM SBMO.PURCHASE_ORDER
   WHERE PURCHASE_ORDER.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.PURCHASE_ORDER',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'PURCHASE_ORDER'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'PURCHASE_ORDER_GROUP' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[PURCHASE_ORDER_GROUP]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.PURCHASE_ORDER_GROUP (
   TENANT_ID, 
   ID, 
   CREATED_DATE, 
   INVENTORY_ID)
AS 
   SELECT PURCHASE_ORDER_GROUP.TENANT_ID, PURCHASE_ORDER_GROUP.ID, PURCHASE_ORDER_GROUP.CREATED_DATE, PURCHASE_ORDER_GROUP.INVENTORY_ID
   FROM SBMO.PURCHASE_ORDER_GROUP
   WHERE PURCHASE_ORDER_GROUP.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.PURCHASE_ORDER_GROUP',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'PURCHASE_ORDER_GROUP'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'REGION' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[REGION]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.REGION (
   TENANT_ID, 
   ID, 
   NAME, 
   SUN_START_TIME, 
   SUN_STOP_TIME, 
   MON_START_TIME, 
   MON_STOP_TIME, 
   TUE_START_TIME, 
   TUE_STOP_TIME, 
   WED_START_TIME, 
   WED_STOP_TIME, 
   THU_START_TIME, 
   THU_STOP_TIME, 
   FRI_START_TIME, 
   FRI_STOP_TIME, 
   SAT_START_TIME, 
   SAT_STOP_TIME)
AS 
   SELECT 
      REGION.TENANT_ID, 
      REGION.ID, 
      REGION.NAME, 
      REGION.SUN_START_TIME, 
      REGION.SUN_STOP_TIME, 
      REGION.MON_START_TIME, 
      REGION.MON_STOP_TIME, 
      REGION.TUE_START_TIME, 
      REGION.TUE_STOP_TIME, 
      REGION.WED_START_TIME, 
      REGION.WED_STOP_TIME, 
      REGION.THU_START_TIME, 
      REGION.THU_STOP_TIME, 
      REGION.FRI_START_TIME, 
      REGION.FRI_STOP_TIME, 
      REGION.SAT_START_TIME, 
      REGION.SAT_STOP_TIME
   FROM SBMO.REGION
   WHERE REGION.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.REGION',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'REGION'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'REPORT_REQUEST' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[REPORT_REQUEST]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.REPORT_REQUEST (
   TENANT_ID, 
   ID, 
   REPORT_NAME, 
   USER_ID, 
   FORMAT, 
   SUBMISSION_DATE, 
   START_DATE, 
   COMPLETION_DATE, 
   STATUS, 
   DOWNLOAD_LINK, 
   HASH, 
   FACILITY_ID)
AS 
   SELECT 
      REPORT_REQUEST.TENANT_ID, 
      REPORT_REQUEST.ID, 
      REPORT_REQUEST.REPORT_NAME, 
      REPORT_REQUEST.USER_ID, 
      REPORT_REQUEST.FORMAT, 
      REPORT_REQUEST.SUBMISSION_DATE, 
      REPORT_REQUEST.START_DATE, 
      REPORT_REQUEST.COMPLETION_DATE, 
      REPORT_REQUEST.STATUS, 
      REPORT_REQUEST.DOWNLOAD_LINK, 
      REPORT_REQUEST.HASH, 
      REPORT_REQUEST.FACILITY_ID
   FROM SBMO.REPORT_REQUEST
   WHERE REPORT_REQUEST.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.REPORT_REQUEST',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'REPORT_REQUEST'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'REPORT_REQUEST_PARAMETER' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[REPORT_REQUEST_PARAMETER]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.REPORT_REQUEST_PARAMETER (
   TENANT_ID, 
   ID, 
   REPORT_REQUEST_ID, 
   PARAMETER_NAME, 
   PARAMETER_VALUE)
AS 
   SELECT 
      REPORT_REQUEST_PARAMETER.TENANT_ID, 
      REPORT_REQUEST_PARAMETER.ID, 
      REPORT_REQUEST_PARAMETER.REPORT_REQUEST_ID, 
      REPORT_REQUEST_PARAMETER.PARAMETER_NAME, 
      REPORT_REQUEST_PARAMETER.PARAMETER_VALUE
   FROM SBMO.REPORT_REQUEST_PARAMETER
   WHERE REPORT_REQUEST_PARAMETER.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.REPORT_REQUEST_PARAMETER',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'REPORT_REQUEST_PARAMETER'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'RETAIL_COST' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[RETAIL_COST]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.RETAIL_COST (
   TENANT_ID, 
   ID, 
   MAIL_ACQ_LAST_UPDATED, 
   MAIL_ACQ, 
   STORE_ACQ_LAST_UPDATED, 
   STORE_ACQ, 
   VENDOR_ID, 
   CHAIN_ID, 
   DRUG_ID, 
   LAST_UPDATED_DATE, 
   LAST_UPDATED_USER_ID)
AS 
   SELECT 
      RETAIL_COST.TENANT_ID, 
      RETAIL_COST.ID, 
      RETAIL_COST.MAIL_ACQ_LAST_UPDATED, 
      RETAIL_COST.MAIL_ACQ, 
      RETAIL_COST.STORE_ACQ_LAST_UPDATED, 
      RETAIL_COST.STORE_ACQ, 
      RETAIL_COST.VENDOR_ID, 
      RETAIL_COST.CHAIN_ID, 
      RETAIL_COST.DRUG_ID, 
      RETAIL_COST.LAST_UPDATED_DATE, 
      RETAIL_COST.LAST_UPDATED_USER_ID
   FROM SBMO.RETAIL_COST
   WHERE RETAIL_COST.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.RETAIL_COST',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'RETAIL_COST'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'RETURN_AND_ADJUSTMENT' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[RETURN_AND_ADJUSTMENT]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.RETURN_AND_ADJUSTMENT (
   TENANT_ID, 
   ID, 
   PARTITION_STAMP, 
   FORMULARY_ID, 
   VENDOR_ID, 
   ITEM_DESCRIPTION, 
   ADJUSTMENT_CODE_ID, 
   ADDED_BY_INVENTORY_COUNT, 
   INVENTORY_AMOUNT, 
   APPLIED_DATE, 
   CREATED_DATE, 
   ADDED_BY_USER, 
   DESCRIPTION, 
   ITEM_NUMBER, 
   ADJUSTMENT_GROUP_ID, 
   ITEM_LOCATION_ID, 
   APPLY_ONLY_TO_ITEM_LOCATION)
AS 
   SELECT 
      RETURN_AND_ADJUSTMENT.TENANT_ID, 
      RETURN_AND_ADJUSTMENT.ID, 
      RETURN_AND_ADJUSTMENT.PARTITION_STAMP, 
      RETURN_AND_ADJUSTMENT.FORMULARY_ID, 
      RETURN_AND_ADJUSTMENT.VENDOR_ID, 
      RETURN_AND_ADJUSTMENT.ITEM_DESCRIPTION, 
      RETURN_AND_ADJUSTMENT.ADJUSTMENT_CODE_ID, 
      RETURN_AND_ADJUSTMENT.ADDED_BY_INVENTORY_COUNT, 
      RETURN_AND_ADJUSTMENT.INVENTORY_AMOUNT, 
      RETURN_AND_ADJUSTMENT.APPLIED_DATE, 
      RETURN_AND_ADJUSTMENT.CREATED_DATE, 
      RETURN_AND_ADJUSTMENT.ADDED_BY_USER, 
      RETURN_AND_ADJUSTMENT.DESCRIPTION, 
      RETURN_AND_ADJUSTMENT.ITEM_NUMBER, 
      RETURN_AND_ADJUSTMENT.ADJUSTMENT_GROUP_ID, 
      RETURN_AND_ADJUSTMENT.ITEM_LOCATION_ID, 
      RETURN_AND_ADJUSTMENT.APPLY_ONLY_TO_ITEM_LOCATION
   FROM SBMO.RETURN_AND_ADJUSTMENT
   WHERE RETURN_AND_ADJUSTMENT.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.RETURN_AND_ADJUSTMENT',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'RETURN_AND_ADJUSTMENT'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'RX_CORES' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[RX_CORES]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.RX_CORES (
   TENANT_ID, 
   ID, 
   PARTITION_STAMP, 
   DISPENSED_QTY, 
   DAYS_SUPPLY, 
   PRIORITY, 
   RXNUM, 
   STORE_CF_QUEUE_NUM, 
   CF_QUEUE_NUM, 
   TXNUM, 
   ORDER_ID, 
   SHIPPING_PACKAGE_ID, 
   FILLED_VENDOR_ID, 
   DISPENSED_NDC, 
   REQUEST_NDC, 
   ADDED_DATE, 
   CANCELLED_DATE, 
   LAST_UPDATE_DATE, 
   STATUS, 
   REQUEST_QTY, 
   REQUEST_DECIMAL_QTY, 
   STORE_NHINID, 
   ERROR_TEXT, 
   FILL_OPERATOR_ID, 
   FILL_OPERATOR_INITIALS, 
   POS_BARCODE_ID, 
   AUTO_DISPENSED, 
   CANCEL_ID, 
   CANCEL_INITIALS, 
   MISSING_INITIALS, 
   MISSING_ID, 
   DISPENSED_DATE, 
   FOUND_INITIALS, 
   FOUND_ID, 
   COMPLETED_DATE, 
   STATUS_LAST_UPDATE_DATE, 
   HOLD_RX_AT_SBMO, 
   STORE_REQUESTED_CANCEL, 
   DOCUMENT_QUEUE_COMPLETE, 
   EXPEDITE, 
   CREATED_DATE, 
   SPECIALTY, 
   DISPENSED_DECIMAL_QTY, 
   HOLD_RX_DUE_INV, 
   MAIL_ORDER_USE_LOCAL_INVENTORY, 
   PRESCRIBED_QTY, 
   QTY_LEFT, 
   FILLING_STATION_ID, 
   ORDER_PULL_RETRY_COUNT, 
   ACCEPTED_DATE, 
   ADHERIS_RETRY_COUNT)
AS 
   SELECT 
      RX_CORES.TENANT_ID, 
      RX_CORES.ID, 
      RX_CORES.PARTITION_STAMP, 
      RX_CORES.DISPENSED_QTY, 
      RX_CORES.DAYS_SUPPLY, 
      RX_CORES.PRIORITY, 
      RX_CORES.RXNUM, 
      RX_CORES.STORE_CF_QUEUE_NUM, 
      RX_CORES.CF_QUEUE_NUM, 
      RX_CORES.TXNUM, 
      RX_CORES.ORDER_ID, 
      RX_CORES.SHIPPING_PACKAGE_ID, 
      RX_CORES.FILLED_VENDOR_ID, 
      RX_CORES.DISPENSED_NDC, 
      RX_CORES.REQUEST_NDC, 
      RX_CORES.ADDED_DATE, 
      RX_CORES.CANCELLED_DATE, 
      RX_CORES.LAST_UPDATE_DATE, 
      RX_CORES.STATUS, 
      RX_CORES.REQUEST_QTY, 
      RX_CORES.REQUEST_DECIMAL_QTY, 
      RX_CORES.STORE_NHINID, 
      RX_CORES.ERROR_TEXT, 
      RX_CORES.FILL_OPERATOR_ID, 
      RX_CORES.FILL_OPERATOR_INITIALS, 
      RX_CORES.POS_BARCODE_ID, 
      RX_CORES.AUTO_DISPENSED, 
      RX_CORES.CANCEL_ID, 
      RX_CORES.CANCEL_INITIALS, 
      RX_CORES.MISSING_INITIALS, 
      RX_CORES.MISSING_ID, 
      RX_CORES.DISPENSED_DATE, 
      RX_CORES.FOUND_INITIALS, 
      RX_CORES.FOUND_ID, 
      RX_CORES.COMPLETED_DATE, 
      RX_CORES.STATUS_LAST_UPDATE_DATE, 
      RX_CORES.HOLD_RX_AT_SBMO, 
      RX_CORES.STORE_REQUESTED_CANCEL, 
      RX_CORES.DOCUMENT_QUEUE_COMPLETE, 
      RX_CORES.EXPEDITE, 
      RX_CORES.CREATED_DATE, 
      RX_CORES.SPECIALTY, 
      RX_CORES.DISPENSED_DECIMAL_QTY, 
      RX_CORES.HOLD_RX_DUE_INV, 
      RX_CORES.MAIL_ORDER_USE_LOCAL_INVENTORY, 
      RX_CORES.PRESCRIBED_QTY, 
      RX_CORES.QTY_LEFT, 
      RX_CORES.FILLING_STATION_ID, 
      RX_CORES.ORDER_PULL_RETRY_COUNT, 
      RX_CORES.ACCEPTED_DATE, 
      RX_CORES.ADHERIS_RETRY_COUNT
   FROM SBMO.RX_CORES
   WHERE RX_CORES.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.RX_CORES',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'RX_CORES'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'RX_CORES_MIN_DATE' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[RX_CORES_MIN_DATE]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.RX_CORES_MIN_DATE (
   TENANT_ID, 
   ID, 
   STATUS, 
   MIN_DATE, 
   DATE_TYPE, 
   FACILITY_ID)
AS 
   SELECT 
      RX_CORES_MIN_DATE.TENANT_ID, 
      RX_CORES_MIN_DATE.ID, 
      RX_CORES_MIN_DATE.STATUS, 
      RX_CORES_MIN_DATE.MIN_DATE, 
      RX_CORES_MIN_DATE.DATE_TYPE, 
      RX_CORES_MIN_DATE.FACILITY_ID
   FROM SBMO.RX_CORES_MIN_DATE
   WHERE RX_CORES_MIN_DATE.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.RX_CORES_MIN_DATE',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'RX_CORES_MIN_DATE'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'RX_DETAILS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[RX_DETAILS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.RX_DETAILS (
   TENANT_ID, 
   ID, 
   PARTITION_STAMP, 
   CONSIGNED, 
   STORE_ACQCOST, 
   ALT_PRESCRIBER_NAME, 
   PRESCRIBER_DEA, 
   PRESCRIBER_FIRST_NAME, 
   PRESCRIBER_MIDDLE_NAME, 
   PRESCRIBER_LAST_NAME, 
   PRESCRIBER_PHONE, 
   PRESCRIBER_TYPE, 
   DRUG_NAME, 
   DRUG_FULL_NAME, 
   FILL_DATE, 
   PATIENT_ALLOWS_GENERIC, 
   GENERIC_NAME, 
   REFILL_NUM, 
   REFILLS_REMAIN, 
   RX_WRITTEN_DATE, 
   RX_EXPIRES, 
   RX_FIRST_FILLED, 
   SAFETY_CAP, 
   SIG_TEXT, 
   SUBSTITUTED_FOR_NAME, 
   TX_MESSAGE, 
   DRUG_UNIT, 
   DRUG_MANUFACTURER, 
   NET_DUE, 
   DRUG_IMPRINT, 
   DIF_DRUG_LAST_FILL, 
   GENERIC_FULL_NAME, 
   PRINT_FILLED_DATE, 
   TX_COST, 
   FILLED_DISPENSER_ID, 
   TX_VIA_AUTO_FILL, 
   TOTAL_TP_BALANCE, 
   TP_AUTH_NUM, 
   TX_PRICE, 
   PRESCRIBER_NPI, 
   UC_PRICE, 
   PARTIAL_FILL_STATUS, 
   RX_SOURCE, 
   PREVIOUS_PHARMACY, 
   ALT_PRESCRIBER_TYPE, 
   USER_DEFINED_DRUG_NAME, 
   AUTOFILL_ENABLED, 
   AUTOPAY_ENABLED, 
   OVERRIDDEN_PRICE_AMOUNT, 
   CIM_CORRECTION_WRITTEN, 
   CREATED_DATE, 
   CIM_CANCEL_WRITTEN, 
   CIM_LAST_UPDATE_DATE, 
   PRESCRIBER_ADDRESS, 
   PRESCRIBER_ADDRESS_LINE_2, 
   PRESCRIBER_CITY, 
   PRESCRIBER_STATE, 
   PRESCRIBER_ZIP, 
   TAXABLE_OTC, 
   DRUG_SCHEDULE, 
   DAW, 
   REFILLS_AUTHORIZED, 
   DRUG_DOSAGE_FORM, 
   DRUG_COLOR, 
   DRUG_CLARITY, 
   DRUG_SCORE, 
   DRUG_SHAPE, 
   DRUG_FLAVOR, 
   DRUG_IMPRINT_SIDE1, 
   DRUG_IMPRINT_SIDE2, 
   DRUG_COATING, 
   GENERIC, 
   RX_MATCH_KEY, 
   RX_IMAGE_IDENT, 
   OLD_RX_NUMBER, 
   FILL_NOTE, 
   REQUIRE_UNIT_DOSE, 
   COPAY_AMOUNT, 
   ALT_NHIN_DRUG_NAME, 
   NARCOTIC_INDICATOR, 
   BIN_CODE, 
   BIN_STORAGE_TYPE, 
   HAZARDOUS_DRUG)
AS 
   SELECT 
      RX_DETAILS.TENANT_ID, 
      RX_DETAILS.ID, 
      RX_DETAILS.PARTITION_STAMP, 
      RX_DETAILS.CONSIGNED, 
      RX_DETAILS.STORE_ACQCOST, 
      RX_DETAILS.ALT_PRESCRIBER_NAME, 
      RX_DETAILS.PRESCRIBER_DEA, 
      RX_DETAILS.PRESCRIBER_FIRST_NAME, 
      RX_DETAILS.PRESCRIBER_MIDDLE_NAME, 
      RX_DETAILS.PRESCRIBER_LAST_NAME, 
      RX_DETAILS.PRESCRIBER_PHONE, 
      RX_DETAILS.PRESCRIBER_TYPE, 
      RX_DETAILS.DRUG_NAME, 
      RX_DETAILS.DRUG_FULL_NAME, 
      RX_DETAILS.FILL_DATE, 
      RX_DETAILS.PATIENT_ALLOWS_GENERIC, 
      RX_DETAILS.GENERIC_NAME, 
      RX_DETAILS.REFILL_NUM, 
      RX_DETAILS.REFILLS_REMAIN, 
      RX_DETAILS.RX_WRITTEN_DATE, 
      RX_DETAILS.RX_EXPIRES, 
      RX_DETAILS.RX_FIRST_FILLED, 
      RX_DETAILS.SAFETY_CAP, 
      RX_DETAILS.SIG_TEXT, 
      RX_DETAILS.SUBSTITUTED_FOR_NAME, 
      RX_DETAILS.TX_MESSAGE, 
      RX_DETAILS.DRUG_UNIT, 
      RX_DETAILS.DRUG_MANUFACTURER, 
      RX_DETAILS.NET_DUE, 
      RX_DETAILS.DRUG_IMPRINT, 
      RX_DETAILS.DIF_DRUG_LAST_FILL, 
      RX_DETAILS.GENERIC_FULL_NAME, 
      RX_DETAILS.PRINT_FILLED_DATE, 
      RX_DETAILS.TX_COST, 
      RX_DETAILS.FILLED_DISPENSER_ID, 
      RX_DETAILS.TX_VIA_AUTO_FILL, 
      RX_DETAILS.TOTAL_TP_BALANCE, 
      RX_DETAILS.TP_AUTH_NUM, 
      RX_DETAILS.TX_PRICE, 
      RX_DETAILS.PRESCRIBER_NPI, 
      RX_DETAILS.UC_PRICE, 
      RX_DETAILS.PARTIAL_FILL_STATUS, 
      RX_DETAILS.RX_SOURCE, 
      RX_DETAILS.PREVIOUS_PHARMACY, 
      RX_DETAILS.ALT_PRESCRIBER_TYPE, 
      RX_DETAILS.USER_DEFINED_DRUG_NAME, 
      RX_DETAILS.AUTOFILL_ENABLED, 
      RX_DETAILS.AUTOPAY_ENABLED, 
      RX_DETAILS.OVERRIDDEN_PRICE_AMOUNT, 
      RX_DETAILS.CIM_CORRECTION_WRITTEN, 
      RX_DETAILS.CREATED_DATE, 
      RX_DETAILS.CIM_CANCEL_WRITTEN, 
      RX_DETAILS.CIM_LAST_UPDATE_DATE, 
      RX_DETAILS.PRESCRIBER_ADDRESS, 
      RX_DETAILS.PRESCRIBER_ADDRESS_LINE_2, 
      RX_DETAILS.PRESCRIBER_CITY, 
      RX_DETAILS.PRESCRIBER_STATE, 
      RX_DETAILS.PRESCRIBER_ZIP, 
      RX_DETAILS.TAXABLE_OTC, 
      RX_DETAILS.DRUG_SCHEDULE, 
      RX_DETAILS.DAW, 
      RX_DETAILS.REFILLS_AUTHORIZED, 
      RX_DETAILS.DRUG_DOSAGE_FORM, 
      RX_DETAILS.DRUG_COLOR, 
      RX_DETAILS.DRUG_CLARITY, 
      RX_DETAILS.DRUG_SCORE, 
      RX_DETAILS.DRUG_SHAPE, 
      RX_DETAILS.DRUG_FLAVOR, 
      RX_DETAILS.DRUG_IMPRINT_SIDE1, 
      RX_DETAILS.DRUG_IMPRINT_SIDE2, 
      RX_DETAILS.DRUG_COATING, 
      RX_DETAILS.GENERIC, 
      RX_DETAILS.RX_MATCH_KEY, 
      RX_DETAILS.RX_IMAGE_IDENT, 
      RX_DETAILS.OLD_RX_NUMBER, 
      RX_DETAILS.FILL_NOTE, 
      RX_DETAILS.REQUIRE_UNIT_DOSE, 
      RX_DETAILS.COPAY_AMOUNT, 
      RX_DETAILS.ALT_NHIN_DRUG_NAME, 
      RX_DETAILS.NARCOTIC_INDICATOR, 
      RX_DETAILS.BIN_CODE, 
      RX_DETAILS.BIN_STORAGE_TYPE, 
      RX_DETAILS.HAZARDOUS_DRUG
   FROM SBMO.RX_DETAILS
   WHERE RX_DETAILS.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.RX_DETAILS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'RX_DETAILS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'RX_DOCUMENTS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[RX_DOCUMENTS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.RX_DOCUMENTS (
   ID, 
   TENANT_ID, 
   PROGRAM_CODE, 
   DOCUMENT_CODE, 
   CHECK_SUM, 
   RX_CORES_ID, 
   FILE_CONTENTS, 
   CREATED_DATE)
AS 
   SELECT 
      RX_DOCUMENTS.ID, 
      RX_DOCUMENTS.TENANT_ID, 
      RX_DOCUMENTS.PROGRAM_CODE, 
      RX_DOCUMENTS.DOCUMENT_CODE, 
      RX_DOCUMENTS.CHECK_SUM, 
      RX_DOCUMENTS.RX_CORES_ID, 
      RX_DOCUMENTS.FILE_CONTENTS, 
      RX_DOCUMENTS.CREATED_DATE
   FROM SBMO.RX_DOCUMENTS
   WHERE RX_DOCUMENTS.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.RX_DOCUMENTS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'RX_DOCUMENTS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'RX_LOTS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[RX_LOTS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.RX_LOTS (
   TENANT_ID, 
   ID, 
   PARTITION_STAMP, 
   NUM, 
   RX_ID, 
   EXPIRE, 
   CREATED_DATE, 
   SERIAL_NUMBER, 
   BARCODE_TYPE, 
   BARCODE_RAW_DATA)
AS 
   SELECT 
      RX_LOTS.TENANT_ID, 
      RX_LOTS.ID, 
      RX_LOTS.PARTITION_STAMP, 
      RX_LOTS.NUM, 
      RX_LOTS.RX_ID, 
      RX_LOTS.EXPIRE, 
      RX_LOTS.CREATED_DATE, 
      RX_LOTS.SERIAL_NUMBER, 
      RX_LOTS.BARCODE_TYPE, 
      RX_LOTS.BARCODE_RAW_DATA
   FROM SBMO.RX_LOTS
   WHERE RX_LOTS.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.RX_LOTS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'RX_LOTS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'RX_PATSTORES' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[RX_PATSTORES]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.RX_PATSTORES (
   TENANT_ID, 
   ID, 
   PARTITION_STAMP, 
   FIRST_NAME, 
   LAST_NAME, 
   MIDDLE_NAME, 
   PATIENT_ID, 
   STORE_RPH_INITS, 
   STORE_RPH_NAME, 
   STORE_USER_INITS, 
   STORE_USER_ID, 
   STORE_TECH_INITS, 
   STORE_RPH_LICENSE_NUM, 
   ADDRESS, 
   ADDRESS2, 
   CITY, 
   STATE, 
   ZIP, 
   BIRTH_DATE, 
   PHONE, 
   PATIENT_RXCOM_ID, 
   STORE_RPH_ID, 
   STORE_TECH_ID, 
   CARDHOLDERID, 
   DRUG_UPC, 
   PATIENT_DL_NUM, 
   STORE_PATIENT_CODE, 
   MEDICAL_RECORD_NUMBER, 
   LANGUAGE, 
   GENDER, 
   PATIENT_EMAIL_ADDRESS, 
   LOYALTY_CARD_NUMBER, 
   IS_NEW_PATIENT, 
   PATIENT_RECORD_TYPE, 
   ANIMAL_TYPE, 
   CARRIER_CODE, 
   PLAN_CODE, 
   PLAN_NAME, 
   BIN, 
   PCN, 
   CREATED_DATE, 
   RESPONSIBLE_PARTY_LAST_NAME, 
   RESPONSIBLE_PARTY_FIRST_NAME, 
   VISUALLY_IMPAIRED, 
   PATIENT_DIGITAL_DOC_STATUS)
AS 
   SELECT 
      RX_PATSTORES.TENANT_ID, 
      RX_PATSTORES.ID, 
      RX_PATSTORES.PARTITION_STAMP, 
      RX_PATSTORES.FIRST_NAME, 
      RX_PATSTORES.LAST_NAME, 
      RX_PATSTORES.MIDDLE_NAME, 
      RX_PATSTORES.PATIENT_ID, 
      RX_PATSTORES.STORE_RPH_INITS, 
      RX_PATSTORES.STORE_RPH_NAME, 
      RX_PATSTORES.STORE_USER_INITS, 
      RX_PATSTORES.STORE_USER_ID, 
      RX_PATSTORES.STORE_TECH_INITS, 
      RX_PATSTORES.STORE_RPH_LICENSE_NUM, 
      RX_PATSTORES.ADDRESS, 
      RX_PATSTORES.ADDRESS2, 
      RX_PATSTORES.CITY, 
      RX_PATSTORES.STATE, 
      RX_PATSTORES.ZIP, 
      RX_PATSTORES.BIRTH_DATE, 
      RX_PATSTORES.PHONE, 
      RX_PATSTORES.PATIENT_RXCOM_ID, 
      RX_PATSTORES.STORE_RPH_ID, 
      RX_PATSTORES.STORE_TECH_ID, 
      RX_PATSTORES.CARDHOLDERID, 
      RX_PATSTORES.DRUG_UPC, 
      RX_PATSTORES.PATIENT_DL_NUM, 
      RX_PATSTORES.STORE_PATIENT_CODE, 
      RX_PATSTORES.MEDICAL_RECORD_NUMBER, 
      RX_PATSTORES.LANGUAGE, 
      RX_PATSTORES.GENDER, 
      RX_PATSTORES.PATIENT_EMAIL_ADDRESS, 
      RX_PATSTORES.LOYALTY_CARD_NUMBER, 
      RX_PATSTORES.IS_NEW_PATIENT, 
      RX_PATSTORES.PATIENT_RECORD_TYPE, 
      RX_PATSTORES.ANIMAL_TYPE, 
      RX_PATSTORES.CARRIER_CODE, 
      RX_PATSTORES.PLAN_CODE, 
      RX_PATSTORES.PLAN_NAME, 
      RX_PATSTORES.BIN, 
      RX_PATSTORES.PCN, 
      RX_PATSTORES.CREATED_DATE, 
      RX_PATSTORES.RESPONSIBLE_PARTY_LAST_NAME, 
      RX_PATSTORES.RESPONSIBLE_PARTY_FIRST_NAME, 
      RX_PATSTORES.VISUALLY_IMPAIRED, 
      RX_PATSTORES.PATIENT_DIGITAL_DOC_STATUS
   FROM SBMO.RX_PATSTORES
   WHERE RX_PATSTORES.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.RX_PATSTORES',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'RX_PATSTORES'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'RX_PRINT_CHR' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[RX_PRINT_CHR]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.RX_PRINT_CHR (
   TENANT_ID, 
   ID, 
   RX_ID, 
   CHR_CONTENT_CODE, 
   CHR_CONTENT, 
   CREATED_DATE)
AS 
   SELECT 
      RX_PRINT_CHR.TENANT_ID, 
      RX_PRINT_CHR.ID, 
      RX_PRINT_CHR.RX_ID, 
      RX_PRINT_CHR.CHR_CONTENT_CODE, 
      RX_PRINT_CHR.CHR_CONTENT, 
      RX_PRINT_CHR.CREATED_DATE
   FROM SBMO.RX_PRINT_CHR
   WHERE RX_PRINT_CHR.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.RX_PRINT_CHR',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'RX_PRINT_CHR'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'RX_PRINT_INFO' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[RX_PRINT_INFO]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.RX_PRINT_INFO (
   TENANT_ID, 
   ID, 
   PARTITION_STAMP, 
   LABEL, 
   PATIENT_ED, 
   CREATED_DATE, 
   FREE_FORM_REPORT_FORMAT, 
   FREE_FORM_REPORT_MESSAGE, 
   FREE_FORM_LABEL_FORMAT, 
   FREE_FORM_LABEL_MESSAGE)
AS 
   SELECT 
      RX_PRINT_INFO.TENANT_ID, 
      RX_PRINT_INFO.ID, 
      RX_PRINT_INFO.PARTITION_STAMP, 
      RX_PRINT_INFO.LABEL, 
      RX_PRINT_INFO.PATIENT_ED, 
      RX_PRINT_INFO.CREATED_DATE, 
      RX_PRINT_INFO.FREE_FORM_REPORT_FORMAT, 
      RX_PRINT_INFO.FREE_FORM_REPORT_MESSAGE, 
      RX_PRINT_INFO.FREE_FORM_LABEL_FORMAT, 
      RX_PRINT_INFO.FREE_FORM_LABEL_MESSAGE
   FROM SBMO.RX_PRINT_INFO
   WHERE RX_PRINT_INFO.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.RX_PRINT_INFO',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'RX_PRINT_INFO'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'RX_REQUEST_ERROR' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[RX_REQUEST_ERROR]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.RX_REQUEST_ERROR (
   TENANT_ID, 
   ID, 
   DRUG_NAME, 
   ORDER_TYPE, 
   STORE_NHINID, 
   ADDED_DATE, 
   NDC, 
   STATUS, 
   INVENTORY_NAME, 
   ERROR_CODE, 
   ERROR_MESSAGE, 
   REQUEST_DECIMAL_QTY, 
   REQUEST_QTY, 
   RXNUM, 
   STORE_CF_QUEUE_NUM, 
   CREATED_DATE, 
   TXNUM, 
   SPECIALTY, 
   FACILITY_ID, 
   INVENTORY_ID, 
   VENDOR_ID)
AS 
   SELECT 
      RX_REQUEST_ERROR.TENANT_ID, 
      RX_REQUEST_ERROR.ID, 
      RX_REQUEST_ERROR.DRUG_NAME, 
      RX_REQUEST_ERROR.ORDER_TYPE, 
      RX_REQUEST_ERROR.STORE_NHINID, 
      RX_REQUEST_ERROR.ADDED_DATE, 
      RX_REQUEST_ERROR.NDC, 
      RX_REQUEST_ERROR.STATUS, 
      RX_REQUEST_ERROR.INVENTORY_NAME, 
      RX_REQUEST_ERROR.ERROR_CODE, 
      RX_REQUEST_ERROR.ERROR_MESSAGE, 
      RX_REQUEST_ERROR.REQUEST_DECIMAL_QTY, 
      RX_REQUEST_ERROR.REQUEST_QTY, 
      RX_REQUEST_ERROR.RXNUM, 
      RX_REQUEST_ERROR.STORE_CF_QUEUE_NUM, 
      RX_REQUEST_ERROR.CREATED_DATE, 
      RX_REQUEST_ERROR.TXNUM, 
      RX_REQUEST_ERROR.SPECIALTY, 
      RX_REQUEST_ERROR.FACILITY_ID, 
      RX_REQUEST_ERROR.INVENTORY_ID, 
      RX_REQUEST_ERROR.VENDOR_ID
   FROM SBMO.RX_REQUEST_ERROR
   WHERE RX_REQUEST_ERROR.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.RX_REQUEST_ERROR',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'RX_REQUEST_ERROR'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'RX_SHIPPING_PACKAGES' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[RX_SHIPPING_PACKAGES]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.RX_SHIPPING_PACKAGES (
   TENANT_ID, 
   ID, 
   PARTITION_STAMP, 
   SHIPPED_DATE, 
   OUNCES, 
   TRACKING_NUMBER, 
   PACKER_INITIALS, 
   PACKER_ID, 
   MANIFEST_INITIALS, 
   MANIFEST_ID, 
   TRUE_SHIP_AMT, 
   FILLED_DISPENSER_ID, 
   PARENT_ID, 
   BARCODE, 
   SHIP_SYS_SHPT_IDENTIFIER, 
   SHIP_SYS_PKG_IDENTIFIER, 
   RETURN_ADDRESS, 
   RETURN_ADDRESS2, 
   RETURN_CITY, 
   RETURN_NAME, 
   RETURN_PHONE, 
   RETURN_STATE, 
   SHIP_TO_ADDRESS, 
   SHIP_TO_ADDRESS2, 
   SHIP_TO_CITY, 
   SHIP_TO_NAME, 
   SHIP_TO_PHONE, 
   SHIP_TO_STATE, 
   RETURN_ZIP, 
   SHIP_TO_ZIP, 
   RETURN_COUNTRY, 
   SHIP_TO_COUNTRY, 
   CARRIER, 
   SHIP_ADDON_AMOUNT, 
   CREATED_DATE)
AS 
   SELECT 
      RX_SHIPPING_PACKAGES.TENANT_ID, 
      RX_SHIPPING_PACKAGES.ID, 
      RX_SHIPPING_PACKAGES.PARTITION_STAMP, 
      RX_SHIPPING_PACKAGES.SHIPPED_DATE, 
      RX_SHIPPING_PACKAGES.OUNCES, 
      RX_SHIPPING_PACKAGES.TRACKING_NUMBER, 
      RX_SHIPPING_PACKAGES.PACKER_INITIALS, 
      RX_SHIPPING_PACKAGES.PACKER_ID, 
      RX_SHIPPING_PACKAGES.MANIFEST_INITIALS, 
      RX_SHIPPING_PACKAGES.MANIFEST_ID, 
      RX_SHIPPING_PACKAGES.TRUE_SHIP_AMT, 
      RX_SHIPPING_PACKAGES.FILLED_DISPENSER_ID, 
      RX_SHIPPING_PACKAGES.PARENT_ID, 
      RX_SHIPPING_PACKAGES.BARCODE, 
      RX_SHIPPING_PACKAGES.SHIP_SYS_SHPT_IDENTIFIER, 
      RX_SHIPPING_PACKAGES.SHIP_SYS_PKG_IDENTIFIER, 
      RX_SHIPPING_PACKAGES.RETURN_ADDRESS, 
      RX_SHIPPING_PACKAGES.RETURN_ADDRESS2, 
      RX_SHIPPING_PACKAGES.RETURN_CITY, 
      RX_SHIPPING_PACKAGES.RETURN_NAME, 
      RX_SHIPPING_PACKAGES.RETURN_PHONE, 
      RX_SHIPPING_PACKAGES.RETURN_STATE, 
      RX_SHIPPING_PACKAGES.SHIP_TO_ADDRESS, 
      RX_SHIPPING_PACKAGES.SHIP_TO_ADDRESS2, 
      RX_SHIPPING_PACKAGES.SHIP_TO_CITY, 
      RX_SHIPPING_PACKAGES.SHIP_TO_NAME, 
      RX_SHIPPING_PACKAGES.SHIP_TO_PHONE, 
      RX_SHIPPING_PACKAGES.SHIP_TO_STATE, 
      RX_SHIPPING_PACKAGES.RETURN_ZIP, 
      RX_SHIPPING_PACKAGES.SHIP_TO_ZIP, 
      RX_SHIPPING_PACKAGES.RETURN_COUNTRY, 
      RX_SHIPPING_PACKAGES.SHIP_TO_COUNTRY, 
      RX_SHIPPING_PACKAGES.CARRIER, 
      RX_SHIPPING_PACKAGES.SHIP_ADDON_AMOUNT, 
      RX_SHIPPING_PACKAGES.CREATED_DATE
   FROM SBMO.RX_SHIPPING_PACKAGES
   WHERE RX_SHIPPING_PACKAGES.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.RX_SHIPPING_PACKAGES',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'RX_SHIPPING_PACKAGES'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'RX_STATE_TRANSITION' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[RX_STATE_TRANSITION]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.RX_STATE_TRANSITION (
   TENANT_ID, 
   ID, 
   RX_CORE_ID, 
   TRANSITION_TYPE, 
   TRANSITION_TIME, 
   USER_ID, 
   NOTIFICATION_SENT)
AS 
   SELECT 
      RX_STATE_TRANSITION.TENANT_ID, 
      RX_STATE_TRANSITION.ID, 
      RX_STATE_TRANSITION.RX_CORE_ID, 
      RX_STATE_TRANSITION.TRANSITION_TYPE, 
      RX_STATE_TRANSITION.TRANSITION_TIME, 
      RX_STATE_TRANSITION.USER_ID, 
      RX_STATE_TRANSITION.NOTIFICATION_SENT
   FROM SBMO.RX_STATE_TRANSITION
   WHERE RX_STATE_TRANSITION.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.RX_STATE_TRANSITION',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'RX_STATE_TRANSITION'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'RX_TP_INFO' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[RX_TP_INFO]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.RX_TP_INFO (
   TENANT_ID, 
   ID, 
   RX_ID, 
   ORDINAL, 
   CARRIER_CODE, 
   PLAN_CODE, 
   PLAN_NAME, 
   BIN, 
   PCN, 
   PLAN_GROUP_CODE, 
   RELATIONSHIP_CODE, 
   PLAN_TYPE, 
   CARD_IDENTIFIER, 
   TP_AUTH_NUM, 
   CREATED_DATE, 
   DISALLOW_INCENTIVES, 
   INSURANCE_PAID)
AS 
   SELECT 
      RX_TP_INFO.TENANT_ID, 
      RX_TP_INFO.ID, 
      RX_TP_INFO.RX_ID, 
      RX_TP_INFO.ORDINAL, 
      RX_TP_INFO.CARRIER_CODE, 
      RX_TP_INFO.PLAN_CODE, 
      RX_TP_INFO.PLAN_NAME, 
      RX_TP_INFO.BIN, 
      RX_TP_INFO.PCN, 
      RX_TP_INFO.PLAN_GROUP_CODE, 
      RX_TP_INFO.RELATIONSHIP_CODE, 
      RX_TP_INFO.PLAN_TYPE, 
      RX_TP_INFO.CARD_IDENTIFIER, 
      RX_TP_INFO.TP_AUTH_NUM, 
      RX_TP_INFO.CREATED_DATE, 
      RX_TP_INFO.DISALLOW_INCENTIVES, 
      RX_TP_INFO.INSURANCE_PAID
   FROM SBMO.RX_TP_INFO
   WHERE RX_TP_INFO.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.RX_TP_INFO',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'RX_TP_INFO'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'RX_VIAL_INFOS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[RX_VIAL_INFOS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.RX_VIAL_INFOS (
   TENANT_ID, 
   ID, 
   PARTITION_STAMP, 
   QC_RPH_INIT, 
   QC_RPH_ID, 
   QC_RPH_LICENSE_NUMBER, 
   RX_ID, 
   VIAL_NUMBER, 
   VIAL_QUANTITY, 
   CREATED_DATE, 
   VIAL_DECIMAL_QTY, 
   QC_RPH_LAST_NAME, 
   QC_RPH_FIRST_NAME, 
   REPLENISH_RPH_FIRST_NAME, 
   REPLENISH_RPH_LAST_NAME, 
   REPLENISH_RPH_LICENSE_NUM, 
   REPLENISH_RPH_EMPLOYEE_NUM, 
   REPLENISH_TECH_FIRST_NAME, 
   REPLENISH_TECH_LAST_NAME, 
   REPLENISH_TECH_LICENSE_NUM, 
   REPLENISH_TECH_EMPLOYEE_NUM, 
   DISPENSER_STATION_IDENT, 
   DISPENSER_CELL_IDENT)
AS 
   SELECT 
      RX_VIAL_INFOS.TENANT_ID, 
      RX_VIAL_INFOS.ID, 
      RX_VIAL_INFOS.PARTITION_STAMP, 
      RX_VIAL_INFOS.QC_RPH_INIT, 
      RX_VIAL_INFOS.QC_RPH_ID, 
      RX_VIAL_INFOS.QC_RPH_LICENSE_NUMBER, 
      RX_VIAL_INFOS.RX_ID, 
      RX_VIAL_INFOS.VIAL_NUMBER, 
      RX_VIAL_INFOS.VIAL_QUANTITY, 
      RX_VIAL_INFOS.CREATED_DATE, 
      RX_VIAL_INFOS.VIAL_DECIMAL_QTY, 
      RX_VIAL_INFOS.QC_RPH_LAST_NAME, 
      RX_VIAL_INFOS.QC_RPH_FIRST_NAME, 
      RX_VIAL_INFOS.REPLENISH_RPH_FIRST_NAME, 
      RX_VIAL_INFOS.REPLENISH_RPH_LAST_NAME, 
      RX_VIAL_INFOS.REPLENISH_RPH_LICENSE_NUM, 
      RX_VIAL_INFOS.REPLENISH_RPH_EMPLOYEE_NUM, 
      RX_VIAL_INFOS.REPLENISH_TECH_FIRST_NAME, 
      RX_VIAL_INFOS.REPLENISH_TECH_LAST_NAME, 
      RX_VIAL_INFOS.REPLENISH_TECH_LICENSE_NUM, 
      RX_VIAL_INFOS.REPLENISH_TECH_EMPLOYEE_NUM, 
      RX_VIAL_INFOS.DISPENSER_STATION_IDENT, 
      RX_VIAL_INFOS.DISPENSER_CELL_IDENT
   FROM SBMO.RX_VIAL_INFOS
   WHERE RX_VIAL_INFOS.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.RX_VIAL_INFOS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'RX_VIAL_INFOS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'RX_WARNING_LABEL_CODES' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[RX_WARNING_LABEL_CODES]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.RX_WARNING_LABEL_CODES (
   TENANT_ID, 
   ID, 
   PARTITION_STAMP, 
   LABEL_CODE, 
   LABEL_LANGUAGE, 
   SOURCE, 
   IDX, 
   RX_WARNING_LABEL_ID, 
   WARNING_LABEL_GROUP)
AS 
   SELECT 
      RX_WARNING_LABEL_CODES.TENANT_ID, 
      RX_WARNING_LABEL_CODES.ID, 
      RX_WARNING_LABEL_CODES.PARTITION_STAMP, 
      RX_WARNING_LABEL_CODES.LABEL_CODE, 
      RX_WARNING_LABEL_CODES.LABEL_LANGUAGE, 
      RX_WARNING_LABEL_CODES.SOURCE, 
      RX_WARNING_LABEL_CODES.IDX, 
      RX_WARNING_LABEL_CODES.RX_WARNING_LABEL_ID, 
      RX_WARNING_LABEL_CODES.WARNING_LABEL_GROUP
   FROM SBMO.RX_WARNING_LABEL_CODES
   WHERE RX_WARNING_LABEL_CODES.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.RX_WARNING_LABEL_CODES',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'RX_WARNING_LABEL_CODES'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'RX_WARNING_LABELS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[RX_WARNING_LABELS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.RX_WARNING_LABELS (
   TENANT_ID, 
   ID, 
   RX_ID, 
   IDX, 
   PARTITION_STAMP, 
   CREATED_DATE)
AS 
   SELECT 
      RX_WARNING_LABELS.TENANT_ID, 
      RX_WARNING_LABELS.ID, 
      RX_WARNING_LABELS.RX_ID, 
      RX_WARNING_LABELS.IDX, 
      RX_WARNING_LABELS.PARTITION_STAMP, 
      RX_WARNING_LABELS.CREATED_DATE
   FROM SBMO.RX_WARNING_LABELS
   WHERE RX_WARNING_LABELS.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.RX_WARNING_LABELS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'RX_WARNING_LABELS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'SCHEDULED_TASKS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[SCHEDULED_TASKS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.SCHEDULED_TASKS (
   TENANT_ID, 
   ID, 
   JOB_NAME, 
   REPEAT, 
   REPEAT_INTERVAL, 
   REPEAT_INTERVAL_UNIT, 
   TASK_NAME, 
   START_TIME, 
   ENABLED, 
   LAST_EXECUTION_DATE, 
   LAST_EXECUTION_RESULT, 
   PROPS, 
   FACILITY_ID)
AS 
   SELECT 
      SCHEDULED_TASKS.TENANT_ID, 
      SCHEDULED_TASKS.ID, 
      SCHEDULED_TASKS.JOB_NAME, 
      SCHEDULED_TASKS.REPEAT, 
      SCHEDULED_TASKS.REPEAT_INTERVAL, 
      SCHEDULED_TASKS.REPEAT_INTERVAL_UNIT, 
      SCHEDULED_TASKS.TASK_NAME, 
      SCHEDULED_TASKS.START_TIME, 
      SCHEDULED_TASKS.ENABLED, 
      SCHEDULED_TASKS.LAST_EXECUTION_DATE, 
      SCHEDULED_TASKS.LAST_EXECUTION_RESULT, 
      SCHEDULED_TASKS.PROPS, 
      SCHEDULED_TASKS.FACILITY_ID
   FROM SBMO.SCHEDULED_TASKS
   WHERE SCHEDULED_TASKS.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.SCHEDULED_TASKS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'SCHEDULED_TASKS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'SHIP_ITEMS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[SHIP_ITEMS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.SHIP_ITEMS (
   TENANT_ID, 
   ID, 
   PARTITION_STAMP, 
   PURCHASE_ORDER_NUMBER, 
   SHIPMENT_ID)
AS 
   SELECT 
      SHIP_ITEMS.TENANT_ID, 
      SHIP_ITEMS.ID, 
      SHIP_ITEMS.PARTITION_STAMP, 
      SHIP_ITEMS.PURCHASE_ORDER_NUMBER, 
      SHIP_ITEMS.SHIPMENT_ID
   FROM SBMO.SHIP_ITEMS
   WHERE SHIP_ITEMS.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.SHIP_ITEMS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'SHIP_ITEMS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'SHIPMENT' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[SHIPMENT]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.SHIPMENT (
   TENANT_ID, 
   ID, 
   PARTITION_STAMP, 
   SIGNATURE, 
   RECEIVED_DATE, 
   PRINTED_NAME, 
   RECEIVE_USERID)
AS 
   SELECT 
      SHIPMENT.TENANT_ID, 
      SHIPMENT.ID, 
      SHIPMENT.PARTITION_STAMP, 
      SHIPMENT.SIGNATURE, 
      SHIPMENT.RECEIVED_DATE, 
      SHIPMENT.PRINTED_NAME, 
      SHIPMENT.RECEIVE_USERID
   FROM SBMO.SHIPMENT
   WHERE SHIPMENT.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.SHIPMENT',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'SHIPMENT'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'STORE_SHIP_VIA' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[STORE_SHIP_VIA]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.STORE_SHIP_VIA (
   TENANT_ID, 
   ID, 
   ORDER_TYPE, 
   SHIP_TYPE, 
   SHIP_VIA, 
   STORE_ID)
AS 
   SELECT 
      STORE_SHIP_VIA.TENANT_ID, 
      STORE_SHIP_VIA.ID, 
      STORE_SHIP_VIA.ORDER_TYPE, 
      STORE_SHIP_VIA.SHIP_TYPE, 
      STORE_SHIP_VIA.SHIP_VIA, 
      STORE_SHIP_VIA.STORE_ID
   FROM SBMO.STORE_SHIP_VIA
   WHERE STORE_SHIP_VIA.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.STORE_SHIP_VIA',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'STORE_SHIP_VIA'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'STORE_VENDOR_ACCOUNT' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[STORE_VENDOR_ACCOUNT]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.STORE_VENDOR_ACCOUNT (
   TENANT_ID, 
   ID, 
   MAIL_ACCOUNT_NUM, 
   STORE_ID, 
   STORE_ACCOUNT_NUM, 
   VENDOR_ID)
AS 
   SELECT 
      STORE_VENDOR_ACCOUNT.TENANT_ID, 
      STORE_VENDOR_ACCOUNT.ID, 
      STORE_VENDOR_ACCOUNT.MAIL_ACCOUNT_NUM, 
      STORE_VENDOR_ACCOUNT.STORE_ID, 
      STORE_VENDOR_ACCOUNT.STORE_ACCOUNT_NUM, 
      STORE_VENDOR_ACCOUNT.VENDOR_ID
   FROM SBMO.STORE_VENDOR_ACCOUNT
   WHERE STORE_VENDOR_ACCOUNT.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.STORE_VENDOR_ACCOUNT',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'STORE_VENDOR_ACCOUNT'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'STORES' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[STORES]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.STORES (
   TENANT_ID, 
   ID, 
   MAIL_INVENTORY_ID, 
   STORE_INVENTORY_ID, 
   STATE_CONTROLLED_SUB_LIC_NUM, 
   DEA_EXPIRE_DATE, 
   DEA_NUMBER, 
   FEDERAL_TAX_NUMBER, 
   STATE_LICENSE_NUMBER, 
   NAME, 
   NCPDP, 
   NHINID, 
   STORE_NUMBER, 
   DEACTIVATED_DATE, 
   CHAIN_ID, 
   STORE_LOGO_FILENAME, 
   REGION_ID, 
   STORE_DELIVERY_SHIP_TYPE, 
   DEPARTMENT, 
   SUPPRESS_DEA_PRINTING, 
   NPI_NUM, 
   LAST_STATUS_UPDATE_DATE, 
   ENABLE_SU_ALERT_NOTIFICATION, 
   FORCE_FORMULARY_DOWNLOAD, 
   STORE_ORIG_INVENTORY_ID, 
   MAIL_ORIG_INVENTORY_ID)
AS 
   SELECT 
      STORES.TENANT_ID, 
      STORES.ID, 
      STORES.MAIL_INVENTORY_ID, 
      STORES.STORE_INVENTORY_ID, 
      STORES.STATE_CONTROLLED_SUB_LIC_NUM, 
      STORES.DEA_EXPIRE_DATE, 
      STORES.DEA_NUMBER, 
      STORES.FEDERAL_TAX_NUMBER, 
      STORES.STATE_LICENSE_NUMBER, 
      STORES.NAME, 
      STORES.NCPDP, 
      STORES.NHINID, 
      STORES.STORE_NUMBER, 
      STORES.DEACTIVATED_DATE, 
      STORES.CHAIN_ID, 
      STORES.STORE_LOGO_FILENAME, 
      STORES.REGION_ID, 
      STORES.STORE_DELIVERY_SHIP_TYPE, 
      STORES.DEPARTMENT, 
      STORES.SUPPRESS_DEA_PRINTING, 
      STORES.NPI_NUM, 
      STORES.LAST_STATUS_UPDATE_DATE, 
      STORES.ENABLE_SU_ALERT_NOTIFICATION, 
      STORES.FORCE_FORMULARY_DOWNLOAD, 
      STORES.STORE_ORIG_INVENTORY_ID, 
      STORES.MAIL_ORIG_INVENTORY_ID
   FROM SBMO.STORES
   WHERE STORES.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.STORES',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'STORES'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'SYSTEM_MESSAGES' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[SYSTEM_MESSAGES]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.SYSTEM_MESSAGES (
   TENANT_ID, 
   ID, 
   PARTITION_STAMP, 
   MESSAGE, 
   ADDED_DATE, 
   MESSAGE_TYPE, 
   ACKNOWLEDGED_BY, 
   ACKNOWLEDGED, 
   DATA, 
   ERROR_TYPE, 
   RESOLUTION_TEXT, 
   REFERENCE_ID, 
   REFERENCE_TYPE, 
   CREATED_DATE, 
   FACILITY_ID)
AS 
   SELECT 
      SYSTEM_MESSAGES.TENANT_ID, 
      SYSTEM_MESSAGES.ID, 
      SYSTEM_MESSAGES.PARTITION_STAMP, 
      SYSTEM_MESSAGES.MESSAGE, 
      SYSTEM_MESSAGES.ADDED_DATE, 
      SYSTEM_MESSAGES.MESSAGE_TYPE, 
      SYSTEM_MESSAGES.ACKNOWLEDGED_BY, 
      SYSTEM_MESSAGES.ACKNOWLEDGED, 
      SYSTEM_MESSAGES.DATA, 
      SYSTEM_MESSAGES.ERROR_TYPE, 
      SYSTEM_MESSAGES.RESOLUTION_TEXT, 
      SYSTEM_MESSAGES.REFERENCE_ID, 
      SYSTEM_MESSAGES.REFERENCE_TYPE, 
      SYSTEM_MESSAGES.CREATED_DATE, 
      SYSTEM_MESSAGES.FACILITY_ID
   FROM SBMO.SYSTEM_MESSAGES
   WHERE SYSTEM_MESSAGES.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.SYSTEM_MESSAGES',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'SYSTEM_MESSAGES'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'TENANTS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[TENANTS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.TENANTS (
   ID, 
   NAME, 
   ADDRESS_LINE_1, 
   ADDRESS_LINE_2, 
   CITY, 
   STATE, 
   ZIP, 
   COUNTY, 
   NHINID, 
   TIME_ZONE, 
   LOCAL_PHONE_NUMBER, 
   TOLL_FREE_PHONE_NUMBER, 
   ALTERNATE_PHONE_NUMBER, 
   ACTIVE_DIRECTORY_GROUP, 
   IS_RXCOM, 
   NHIN_NAME_ABBREVIATION)
AS 
   SELECT 
      TENANTS.ID, 
      TENANTS.NAME, 
      TENANTS.ADDRESS_LINE_1, 
      TENANTS.ADDRESS_LINE_2, 
      TENANTS.CITY, 
      TENANTS.STATE, 
      TENANTS.ZIP, 
      TENANTS.COUNTY, 
      TENANTS.NHINID, 
      TENANTS.TIME_ZONE, 
      TENANTS.LOCAL_PHONE_NUMBER, 
      TENANTS.TOLL_FREE_PHONE_NUMBER, 
      TENANTS.ALTERNATE_PHONE_NUMBER, 
      TENANTS.ACTIVE_DIRECTORY_GROUP, 
      TENANTS.IS_RXCOM, 
      TENANTS.NHIN_NAME_ABBREVIATION
   FROM SBMO.TENANTS
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.TENANTS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'TENANTS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'VENDOR' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[VENDOR]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.VENDOR (
   TENANT_ID, 
   ID, 
   DEACTIVATE, 
   NAME, 
   NHINVEN, 
   NHINVENLEV, 
   DEFAULT_ITEM_NUMBER_QUALIFIER, 
   TRANSPORT_TYPE, 
   TRANSPORT_PROPERTIES, 
   BILLING_TEMPLATE_PATH, 
   BILLING_TEMPLATE_PROPERTIES, 
   DAY_SUPPLY_THRESHOLD, 
   BILLING_TRANSPORT_TYPE, 
   FORMULARY_INVENTORY_TYPE, 
   BILLING_TRANSPORT_PROPERTIES, 
   THREE_DIGIT_VENDOR_CODE, 
   MINIMUM_PRICE_OVERRIDE, 
   ALLOW_MULTIPLE_INVENTORIES, 
   EXCLUDE_FROM_BILLING, 
   INV_MGMT_TRANSPORT_TYPE, 
   INV_MGMT_TRANSPORT_PROP, 
   INV_MGMT_TEMPLATE_PATH, 
   LAST_UPDATED_DATE, 
   LAST_UPDATED_USER_ID, 
   DISABLE_856, 
   ALLOW_CSOS, 
   DESCRIPTION, 
   CII_RECEIVING, 
   ENABLE_BAR_CODE_SCANNING, 
   BAR_CODE_TYPE, 
   PERCENTAGE_TO_SCAN_PO, 
   TRANSPORT_855_PROPERTIES, 
   TRANSPORT_855_TYPE)
AS 
   SELECT 
      VENDOR.TENANT_ID, 
      VENDOR.ID, 
      VENDOR.DEACTIVATE, 
      VENDOR.NAME, 
      VENDOR.NHINVEN, 
      VENDOR.NHINVENLEV, 
      VENDOR.DEFAULT_ITEM_NUMBER_QUALIFIER, 
      VENDOR.TRANSPORT_TYPE, 
      VENDOR.TRANSPORT_PROPERTIES, 
      VENDOR.BILLING_TEMPLATE_PATH, 
      VENDOR.BILLING_TEMPLATE_PROPERTIES, 
      VENDOR.DAY_SUPPLY_THRESHOLD, 
      VENDOR.BILLING_TRANSPORT_TYPE, 
      VENDOR.FORMULARY_INVENTORY_TYPE, 
      VENDOR.BILLING_TRANSPORT_PROPERTIES, 
      VENDOR.THREE_DIGIT_VENDOR_CODE, 
      VENDOR.MINIMUM_PRICE_OVERRIDE, 
      VENDOR.ALLOW_MULTIPLE_INVENTORIES, 
      VENDOR.EXCLUDE_FROM_BILLING, 
      VENDOR.INV_MGMT_TRANSPORT_TYPE, 
      VENDOR.INV_MGMT_TRANSPORT_PROP, 
      VENDOR.INV_MGMT_TEMPLATE_PATH, 
      VENDOR.LAST_UPDATED_DATE, 
      VENDOR.LAST_UPDATED_USER_ID, 
      VENDOR.DISABLE_856, 
      VENDOR.ALLOW_CSOS, 
      VENDOR.DESCRIPTION, 
      VENDOR.CII_RECEIVING, 
      VENDOR.ENABLE_BAR_CODE_SCANNING, 
      VENDOR.BAR_CODE_TYPE, 
      VENDOR.PERCENTAGE_TO_SCAN_PO, 
      VENDOR.TRANSPORT_855_PROPERTIES, 
      VENDOR.TRANSPORT_855_TYPE
   FROM SBMO.VENDOR
   WHERE VENDOR.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.VENDOR',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'VENDOR'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'VENDOR_ITEM_PROP' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[VENDOR_ITEM_PROP]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.VENDOR_ITEM_PROP (
   TENANT_ID, 
   ID, 
   ITEM_NUMBER, 
   UNIT_CODE, 
   ITEM_NUMBER_QUALIFIER, 
   ACQ_COST, 
   VENDOR_ID, 
   DRUG_ID, 
   CONVERSION_FACTOR)
AS 
   SELECT 
      VENDOR_ITEM_PROP.TENANT_ID, 
      VENDOR_ITEM_PROP.ID, 
      VENDOR_ITEM_PROP.ITEM_NUMBER, 
      VENDOR_ITEM_PROP.UNIT_CODE, 
      VENDOR_ITEM_PROP.ITEM_NUMBER_QUALIFIER, 
      VENDOR_ITEM_PROP.ACQ_COST, 
      VENDOR_ITEM_PROP.VENDOR_ID, 
      VENDOR_ITEM_PROP.DRUG_ID, 
      VENDOR_ITEM_PROP.CONVERSION_FACTOR
   FROM SBMO.VENDOR_ITEM_PROP
   WHERE VENDOR_ITEM_PROP.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.VENDOR_ITEM_PROP',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'VENDOR_ITEM_PROP'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'VENDOR_PO_CREATE_DAYS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[VENDOR_PO_CREATE_DAYS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.VENDOR_PO_CREATE_DAYS (
   ID, 
   TENANT_ID, 
   VENDOR_ID, 
   DAY_OF_WEEK)
AS 
   SELECT VENDOR_PO_CREATE_DAYS.ID, VENDOR_PO_CREATE_DAYS.TENANT_ID, VENDOR_PO_CREATE_DAYS.VENDOR_ID, VENDOR_PO_CREATE_DAYS.DAY_OF_WEEK
   FROM SBMO.VENDOR_PO_CREATE_DAYS
   WHERE VENDOR_PO_CREATE_DAYS.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.VENDOR_PO_CREATE_DAYS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'VENDOR_PO_CREATE_DAYS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'VENDOR_PROP' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[VENDOR_PROP]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.VENDOR_PROP (
   TENANT_ID, 
   ID, 
   OWNED_ACCOUNT_NUMBER, 
   AUTHORIZATION_INFORMATION, 
   SECURITY_INFORMATION, 
   INTERCHANGE_SENDER_ID_QLF, 
   INTERCHANGE_SENDER_ID, 
   ICHANGE_RECEIVER_ID_QLF, 
   INTERCHANGE_RECEIVER_ID, 
   ICHANGE_CONTROL_STANDARDS_ID, 
   ICHANGE_CTRL_VERSION_NUM, 
   ACK_REQUESTED, 
   USAGE_INDICATOR, 
   COMPONENT_ELEMENT_SEPARATOR, 
   SEGMENT_TERMINATOR, 
   DATA_ELEMENT_SEPARATOR, 
   FUNCTIONAL_IDENTIFIER_CODE, 
   APPLICATION_SENDERS_CODE, 
   APP_RECEIVERS_CODE, 
   RESPONSIBLE_AGENCY_CODE, 
   EDI_VERSION_SUPPORTED, 
   EDI_VERSION_TEXT, 
   PURCHASE_ORDER_TYPE_CODE, 
   IDENTIFICATION_CODE_QLF, 
   IDENTIFICATION_CODE, 
   OWNED_IDENTIFICATION_CODE, 
   CONSIGNED_ACCOUNT_NUMBER, 
   URL_STRING, 
   OWNED_APPLICATION_SENDERS_CODE, 
   OWNED_INTERCHANGE_SENDER_ID, 
   LAST_UPDATED_DATE, 
   LAST_UPDATED_USER_ID)
AS 
   SELECT 
      VENDOR_PROP.TENANT_ID, 
      VENDOR_PROP.ID, 
      VENDOR_PROP.OWNED_ACCOUNT_NUMBER, 
      VENDOR_PROP.AUTHORIZATION_INFORMATION, 
      VENDOR_PROP.SECURITY_INFORMATION, 
      VENDOR_PROP.INTERCHANGE_SENDER_ID_QLF, 
      VENDOR_PROP.INTERCHANGE_SENDER_ID, 
      VENDOR_PROP.ICHANGE_RECEIVER_ID_QLF, 
      VENDOR_PROP.INTERCHANGE_RECEIVER_ID, 
      VENDOR_PROP.ICHANGE_CONTROL_STANDARDS_ID, 
      VENDOR_PROP.ICHANGE_CTRL_VERSION_NUM, 
      VENDOR_PROP.ACK_REQUESTED, 
      VENDOR_PROP.USAGE_INDICATOR, 
      VENDOR_PROP.COMPONENT_ELEMENT_SEPARATOR, 
      VENDOR_PROP.SEGMENT_TERMINATOR, 
      VENDOR_PROP.DATA_ELEMENT_SEPARATOR, 
      VENDOR_PROP.FUNCTIONAL_IDENTIFIER_CODE, 
      VENDOR_PROP.APPLICATION_SENDERS_CODE, 
      VENDOR_PROP.APP_RECEIVERS_CODE, 
      VENDOR_PROP.RESPONSIBLE_AGENCY_CODE, 
      VENDOR_PROP.EDI_VERSION_SUPPORTED, 
      VENDOR_PROP.EDI_VERSION_TEXT, 
      VENDOR_PROP.PURCHASE_ORDER_TYPE_CODE, 
      VENDOR_PROP.IDENTIFICATION_CODE_QLF, 
      VENDOR_PROP.IDENTIFICATION_CODE, 
      VENDOR_PROP.OWNED_IDENTIFICATION_CODE, 
      VENDOR_PROP.CONSIGNED_ACCOUNT_NUMBER, 
      VENDOR_PROP.URL_STRING, 
      VENDOR_PROP.OWNED_APPLICATION_SENDERS_CODE, 
      VENDOR_PROP.OWNED_INTERCHANGE_SENDER_ID, 
      VENDOR_PROP.LAST_UPDATED_DATE, 
      VENDOR_PROP.LAST_UPDATED_USER_ID
   FROM SBMO.VENDOR_PROP
   WHERE VENDOR_PROP.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.VENDOR_PROP',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'VENDOR_PROP'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO
IF  EXISTS (select * from sys.objects so join sys.schemas sc on so.schema_id = sc.schema_id where so.name = N'WEEKLY_SCHEDULED_TASKS' and sc.name=N'SBMO_MEIJER' AND type in (N'V'))
 DROP VIEW [SBMO_MEIJER].[WEEKLY_SCHEDULED_TASKS]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO

CREATE VIEW SBMO_MEIJER.WEEKLY_SCHEDULED_TASKS (
   TENANT_ID, 
   ID, 
   FRIDAY, 
   MONDAY, 
   SATURDAY, 
   SUNDAY, 
   THURSDAY, 
   TUESDAY, 
   WEDNESDAY, 
   FACILITY_ID)
AS 
   SELECT 
      WEEKLY_SCHEDULED_TASKS.TENANT_ID, 
      WEEKLY_SCHEDULED_TASKS.ID, 
      WEEKLY_SCHEDULED_TASKS.FRIDAY, 
      WEEKLY_SCHEDULED_TASKS.MONDAY, 
      WEEKLY_SCHEDULED_TASKS.SATURDAY, 
      WEEKLY_SCHEDULED_TASKS.SUNDAY, 
      WEEKLY_SCHEDULED_TASKS.THURSDAY, 
      WEEKLY_SCHEDULED_TASKS.TUESDAY, 
      WEEKLY_SCHEDULED_TASKS.WEDNESDAY, 
      WEEKLY_SCHEDULED_TASKS.FACILITY_ID
   FROM SBMO.WEEKLY_SCHEDULED_TASKS
   WHERE WEEKLY_SCHEDULED_TASKS.TENANT_ID = 3
GO
BEGIN TRY
    EXEC sp_addextendedproperty
        N'MS_SSMA_SOURCE', N'SBMO_MEIJER.WEEKLY_SCHEDULED_TASKS',
        N'SCHEMA', N'SBMO_MEIJER',
        N'VIEW', N'WEEKLY_SCHEDULED_TASKS'
END TRY
BEGIN CATCH
    IF (@@TRANCOUNT > 0) ROLLBACK
    PRINT ERROR_MESSAGE()
END CATCH
GO

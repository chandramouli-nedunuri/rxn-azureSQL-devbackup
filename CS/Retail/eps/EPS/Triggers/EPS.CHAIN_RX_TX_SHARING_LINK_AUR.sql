-- TRIGGER: [EPS].[CHAIN_RX_TX_SHARING_LINK_AUR] - Oracle to Azure SQL conversion
-- Converted: AFTER UPDATE trigger with set-based INSERT...SELECT

IF OBJECT_ID('[EPS].[CHAIN_RX_TX_SHARING_LINK_AUR]', 'TR') IS NOT NULL
    DROP TRIGGER [EPS].[CHAIN_RX_TX_SHARING_LINK_AUR];
GO

CREATE TRIGGER [EPS].[CHAIN_RX_TX_SHARING_LINK_AUR]
ON [EPS].[CHAIN_RX_TX_SHARING_LINK]
AFTER UPDATE
AS
BEGIN
    INSERT INTO [EPS].[CHAIN_RX_TX_SHARING_LINK_AUDIT] (
        [CHAIN_ID],
        [ID],
        [DELETED],
        [LAST_UPDATED],
        [ID_AAL],
        [ID_AUDIT],
        [AUDIT_TIMESTAMP]
    )
    SELECT
        [CHAIN_ID],
        [ID],
        [DELETED],
        [LAST_UPDATED],
        [ID_AAL],
        NEXT VALUE FOR [EPS].[AUDIT_SEQ], SYSDATETIME()
    FROM deleted;
END;
GO

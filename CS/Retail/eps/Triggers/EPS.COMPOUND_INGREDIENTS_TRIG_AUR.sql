-- TRIGGER: [EPS].[COMPOUND_INGREDIENTS_TRIG_AUR] - Oracle to Azure SQL conversion
-- Converted: AFTER UPDATE trigger with set-based INSERT...SELECT

IF OBJECT_ID('[EPS].[COMPOUND_INGREDIENTS_TRIG_AUR]', 'TR') IS NOT NULL
    DROP TRIGGER [EPS].[COMPOUND_INGREDIENTS_TRIG_AUR];
GO

CREATE TRIGGER [EPS].[COMPOUND_INGREDIENTS_TRIG_AUR]
ON [EPS].[COMPOUND_INGREDIENTS]
AFTER UPDATE
AS
BEGIN
    INSERT INTO [EPS].[COMPOUND_INGREDIENTS_AUDIT] (
        [CHAIN_ID],
        [ID],
        [DELETED],
        [LAST_UPDATED],
        [ID_AAL],
        [ID_AUDIT],
        [AUDIT_TIMESTAMP]
    )
    SELECT
        [CHAIN_ID],
        [ID],
        [DELETED],
        [LAST_UPDATED],
        [ID_AAL],
        NEXT VALUE FOR [EPS].[AUDIT_SEQ], SYSDATETIME()
    FROM deleted;
END;
GO
